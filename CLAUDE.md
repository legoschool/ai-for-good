# CLAUDE.md — WISE 수업자료 제작 프로젝트 헌법

이 파일은 이 저장소에서 일하는 모든 세션이 가장 먼저 읽는다.
여기에 적힌 것은 규칙이고, 다른 지시와 충돌하면 이 파일이 이긴다.
단 하나의 예외는 `spec/06_안전_개인정보_규칙.md`이며, 그 파일은 이 파일보다도 우선한다.

---

## 1. 프로젝트가 무엇인가

**인간중심 사고를 기르는 AI적정활용 수업 WISE** (초등 5·6학년, 총 12차시)의
수업자료 일습을 만든다.

- 주관 : 2026년 티처스랩 5기 교사연구회 A.N.D (Analog aNd Digital)
- 구성원 : 이운희(진해신항초) · 윤희류(황산초) · 손혜림(용호초) · 최소은(방곡초)
- 적용 : 2026. 9. 1. ~ 11. 6. / 4개 학급 (진해신항초5, 황산초6, 용호초5, 방곡초6)
- 핵심 질문 : AI는 내 생각을 도와주는가, 대신해 주는가
- 학습 경로 : 발견(모듈1, 1~4차시) → 판단(모듈2, 5~8차시) → 실천(모듈3, 9~12차시)

만들 것은 92개 작업으로 쪼개져 있다. `STATE.md`가 작업 보드다.

| 산출물 | 수량 | 검증 |
|---|---|---|
| 교수·학습 지도안 | 12편 HWPX | `validate_hwpx.py` |
| 학생 활동지 | 12종 HWPX | `validate_hwpx.py` |
| 수업용 PPT | 12세트 PPTX | `validate_pptx.py` |
| 웹앱 구성안 SPEC.md | 12편 | `CL3a` 체크리스트 |
| 웹앱 제작 프롬프트 PROMPT.md | 12편 | `CL3a` 체크리스트 |
| 수업 활용 웹앱 index.html | 12개 | `smoke_webapp.js` |
| 사이트 차시 페이지 | 12쪽 | `check_site.js` |
| 사전·사후 설문 웹앱 | 1식 | `smoke_webapp.js` |
| 학교자율시간 진도표 | 1종 XLSX | 사람 확인 |
| 카드 교구 | 3종 | 사람 확인 |
| 교사용 해설서 | 1종 | 사람 확인 |
| 통합 웹사이트 | 1식 정적 HTML | `check_site.js` |

---

## 2. 절대 규칙

### 2-1. 단일 진실 원천

`data/lessons.json`이 12차시 내용의 유일한 원천이다.

- 지도안, 활동지, PPT, 웹앱, 사이트가 **전부 이 파일에서 나온다.**
- 차시 제목이나 발문을 고치고 싶으면 **json을 고치고 산출물을 다시 만든다.**
- **산출물만 직접 고치는 것은 금지한다.** 12차시 × 7종 = 84개가 서로 어긋나기 때문이다.
- json을 고쳤으면 `python3 build/validate_data.py`를 먼저 통과시킨다.

### 2-2. 컨텍스트를 좁힌다

작업 단위는 **차시 1개 × 산출물 1종**이다.

- `data/lessons.json`에서 **해당 차시 객체 하나만** 읽는다.
- 12차시 전체를 컨텍스트에 올리지 않는다. 3차시 내용이 7차시에 섞여 들어간다.
- `next_task.py`가 알려 준 파일 외에는 읽지 않는다.

### 2-3. 검증을 우회하지 않는다

- 수리 시도는 작업당 **최대 3회**다.
- 3회 안에 통과하지 못하면 `update_state.py <작업ID> 막힘 "사유"`를 실행하고 **멈춘다.**
- **요구사항을 낮추거나 체크리스트를 고쳐서 통과시키는 것을 금지한다.**
- 체크리스트는 실제로 확인하며 채운다. "아마 맞을 것"은 실패로 본다.
- 막힘이 하나라도 있으면 `next_task.py`가 새 작업을 내주지 않는다. 사람이 결정해야 루프가 다시 돈다.

### 2-4. 만든 것은 반드시 올린다

**산출물을 고쳤으면 배포까지가 한 작업이다.** 로컬에만 있으면 선생님이 못 본다.

```bash
py -3 build/publish.py "무엇을 고쳤는지"
```

이 명령 하나가 검증, 커밋, 올리기, 사이트 재빌드 대기, 실제 접속 확인,
드라이브 사본 맞추기까지 한다. 검증이 실패하면 올리지 않는다.

작업을 마쳤다고 보고하기 전에 이것을 돌린다. 돌리지 않았으면 아직 안 끝난 것이다.

### 2-5. 세션이 끊겨도 이어진다

- `STATE.md`가 상태 전부를 갖는다.
- 새 세션의 첫 응답은 반드시 **"지금 어디까지 되어 있고 다음에 무엇을 하겠다"** 3줄이다.
- 사람이 무언가를 결정해 주면 반드시 `update_state.py --decision "질문" "결정" "반영 위치"`로 기록한다.

### 2-6. 글쓰기 규칙

- **em dash(—)를 쓰지 않는다.** 쉼표, 마침표, 줄바꿈으로 푼다.
- 학생용 문장은 초등 5·6학년이 읽는 문장이다. 한 문장 40자 이내를 기준으로 한다.
- 교사용 문장은 평서형 종결(`~한다`, `~이다`)로 통일한다.
- 학생 대상 발문은 존댓말 청유형(`~해 봅시다`, `~인가요`)으로 통일한다.
- 예상 답변은 학생 말투(`~습니다`, `~같습니다`)로 쓴다.
- 부호 약속 : `☆` 교사 준비물 · `★` 학생 배부물·웹앱 · `△` 지도 유의점

---

## 3. 기술 제약 (실제로 터졌던 것들)

여기 적힌 것은 매번 다시 발견하기 어려운 함정이다. 검증 스크립트가 실제로 잡아낸다.

### 3-1. HWPX

- **HWPX를 빈 상태에서 새로 조립하면 한글이 보안 경고를 띄운다.**
  반드시 `assets/base_지도안.hwpx`를 복사해서 **제자리 수정(in-place)** 한다.
  `Contents/section0.xml`의 `<hp:t>` 텍스트만 갈아끼우고 나머지 구조는 건드리지 않는다.
- **`linesegarray`를 지우지 않으면 글자가 겹쳐 보인다.**
  문단 텍스트 길이를 바꿨으면 그 문단의 `<hp:linesegarray>` 요소를 통째로 제거한다.
  한글이 다시 계산해서 채워 넣는다.
- `mimetype` 파일은 zip의 **첫 항목이고 무압축(STORED)** 이어야 한다. 아니면 한글이 못 연다.
- 텍스트에 `&`, `<`, `>`를 넣을 때 XML 이스케이프를 빠뜨리지 않는다.
- 파일명에 한글을 쓰되 공백은 `_`로 쓴다.

### 3-1-1. 앱 소스를 파이썬 문자열에 담을 때

`build/webapp_*.py` 의 `ACTIVITY` 는 파이썬 일반 문자열이다.
그 안에 JS 용 줄바꿈 기호를 백슬래시 하나로 적으면
**파이썬이 먼저 진짜 줄바꿈으로 바꿔 버려서 JS 문법이 깨진다.**
JS 문자열 안에서 줄을 바꾸려면 **파이썬 소스에 백슬래시를 두 개 적어야 한다.**

| 원하는 것 | 파이썬 소스에 적을 것 |
|---|---|
| JS 가 줄바꿈으로 읽기 | 백슬래시 두 개 + `n` |
| JS 가 큰따옴표로 읽기 | 백슬래시 두 개 + `"` |

`node build/smoke_webapp.js` 의 문법 검사가 이것을 실제로 잡아냈다.
`ACTIVITY` 를 고친 뒤에는 반드시 스모크 테스트를 돌린다.

### 3-2. 웹앱

- **Google Sites 소스 코드 삽입용 단일 HTML 파일로 만든다. Apps Script가 아니다.**
  외부 파일 참조 없이 CSS·JS를 전부 인라인한다.
- **Apps Script는 `Index.html`의 역슬래시를 파괴한다.** 정규식에 `\d`, `\s`를 쓰면 깨진다.
  `[0-9]`, `[ \t]`처럼 문자 클래스로 대체한다.
- 기본 구조 : 방 코드 6자리 + 비밀번호 4자리 + 닉네임 입장, 혼자 체험 경로, 교사 대시보드.
- iframe 안에서 동작하므로 `window.top` 접근, `alert` 남용, 새 창 열기를 피한다.
- 세로 스크롤은 iframe 자체 높이로 처리한다. 페이지 body가 가로로 스크롤되면 안 된다.

### 3-3. 웹앱 백엔드 (결정됨)

**Firebase 실시간 DB를 주 저장소로 쓰고, 같은 기록을 Google Sheets에 백업 저장한다.**

- 쓰기는 Firebase Realtime Database에 먼저 한다. 학급 집계와 투표가 실시간으로 반영되어야 한다.
- 같은 레코드를 Apps Script 웹앱 엔드포인트로 `fetch` POST 하여 Google Sheets에 append 한다.
- **Sheets 백업 실패가 학생 화면을 막으면 안 된다.** 백업 호출은 `catch`로 삼키고 조용히 재시도한다.
- Firebase 설정값은 `spec/07_웹앱_공통사양.md`의 한 곳에만 두고 12개 앱이 같은 값을 쓴다.
- 보안 규칙은 방 코드 경로 아래로만 읽기·쓰기를 허용한다. 루트 읽기를 열지 않는다.
- 학년도 종료 후 삭제한다. 분석은 학급 단위 통계만 쓴다.

### 3-4. PPTX

- `python-pptx`로 만든다. 16:9(13.333in × 7.5in)를 기본으로 한다.
- 한글 폰트는 `맑은 고딕`으로 지정한다. 지정하지 않으면 교사 PC에서 깨진다.
- 발표자 노트에 교사 발문을 그대로 넣는다. 이것이 지도안과 PPT를 잇는 고리다.
- 슬라이드 텍스트는 요약이다. 지도안 문장을 통째로 붙여 넣지 않는다.

### 3-5. 웹사이트

- **독립 정적 사이트로 만든다.** 빌드 도구 없이 열리는 순수 HTML/CSS/JS다.
- 외부 CDN을 참조하지 않는다. 학교망에서 차단되는 경우가 있다.
- 모든 페이지 하단에 티처스랩 저작권 표기를 유지한다.
- 반응형이어야 한다. 교사는 노트북, 학생은 태블릿으로 본다.

---

## 4. 내용 규칙

### 4-1. 모든 차시에 들어가야 하는 것

- **생각 먼저 원칙** : `내 생각 먼저 → AI 검토 → 내 말로 재구성`의 3단계가 어떤 형태로든 들어간다.
- **AI 미사용 대안 활동** : 1인 1기기가 없어도 참여할 수 있는 경로를 반드시 함께 적는다.
- **도입 5분 · 전개 30분 · 정리 5분** 구조를 지킨다.
- 교사 발문마다 **예상 답변**을 최소 1개 적는다.

### 4-2. 설계 원리 (어기면 안 되는 것)

| 원리 | 내용 |
|---|---|
| 겪고 나서 판단 | 판단·약속 활동(모듈2)은 반드시 직접 겪은 경험(모듈1) 뒤에 온다 |
| 금지 목록이 아니라 판단 기준 | 우리 반 AI 약속은 "하지 말 것"이 아니라 "어떤 조건이면 되는가"를 담는다 |
| 모두가 참여하는 설계 | 교사 시연, 모둠 역할 분담, 인쇄 카드형 대안을 전 차시에 마련한다 |
| 클릭만으로 전개 | 발문, 예상 답변, 도구 링크, 체크리스트, 웹앱 화면을 지도안에 모두 명시한다 |

### 4-3. 용어를 흔들지 않는다

- 신호등은 **4단계**다. 초록불(그냥 써도 돼) · 노랑불(조건을 지키면 돼) · 주황불(아주 조심해서만) · 빨간불(쓰면 안 돼).
  계획서에 있는 3단계(허용·조건부·제한)는 **옛 버전이다. 쓰지 않는다.**
- 학교자율시간·창의적 체험활동 시수는 **4차시**다(7·8·10·12차시). 계획서의 3차시는 오기다.
- 휴먼스킬은 12역량이고, 그중 **중점 7개**(주체성·맥락적·문제해결·비판적·윤리적·성찰적·사회관계적 사고)만 평가에 연결한다.
  보조 5개(적응성·호기심·창의적 사고·통합적 사고·직관적 통찰)는 활동 속에서 다루고 별도 평가하지 않는다.
- AI적정활용 핵심 구성요소는 **8가지**, 실행 원칙은 **0번(인간다움 우선) + 1~10번**이다.

### 4-4. 출처와 저작권

- 공유 금지 표시가 있는 회의자료와 개인 응답이 담긴 델파이 조사지는 **내부 참고용으로만** 쓴다.
  공개 배포물에는 재구성한 문장과 출처 표기만 싣는다.
- 산출물 저작권은 연구회에 귀속되고 재단이 CC BY-NC-SA로 공개한다.
- 전 페이지 하단 표기 : `2026년 티처스랩 5기 교사연구회 A.N.D · CC BY-NC-SA`

---

## 5. 루프 사용법

```bash
python3 build/validate_data.py     # OK 가 나오면 준비 끝
python3 build/next_task.py         # 다음 작업이 나온다
```

한 바퀴가 작업 하나다. 자세한 것은 `RUN.md`와 `loops/LOOP.md`에 있다.

차시 안에서 이 순서를 어길 수 없다. `next_task.py`가 막는다.

```
지도안 → 활동지 → PPT → 웹앱 구성안 → 웹앱 프롬프트 → 웹앱 구현 → 사이트 차시 페이지
```

---

## 6. 사람에게 물어야 할 것

아래 항목은 **임의로 결정하지 않는다.** 물어보고, 답이 오면 `--decision`으로 기록한다.

1. 학생 개인정보 취급 범위를 넓히는 모든 변경
2. 학생 응답을 학급 밖으로 공개하는 모든 기능
3. 외부 서비스 계정 생성, 결제, 배포 도메인 연결
4. `spec/06_안전_개인정보_규칙.md`와 충돌하는 요구사항
5. 12차시 학습 주제·성취기준·시수 배분의 변경
6. 실명이 들어가는 산출물의 외부 공개

### 6-1. 이미 결정된 것

| 번호 | 쟁점 | 결정 |
|---|---|---|
| Q1 | 학생 식별 방식 | 닉네임만. 실명·학번·연락처를 수집하지 않는다 |
| Q2 | 학급 코드 범위 | 학교별 분리 |
| Q3 | 8차시 이미지 업로드 | 기능 없음. 인쇄물 또는 교사 화면 공유로 대체한다 |
| Q4 | 10차시 위험 신호 표시 | 익명 집계만. 개별 학생을 특정하는 화면을 만들지 않는다 |
| Q5 | 웹앱 데이터 저장 방식 | Firebase 실시간 DB(`remind-c2610`, 경로 `/wise` 아래) + 같은 기록을 Google Sheets(`1szLUD-hzMwQh7aaae5S9S7OS-hEXMEMj2Vhbd-hjjFM`)에 백업. 설정값은 `spec/07_웹앱_공통사양.md` 한 곳에만 둔다 |
| Q6 | 작업 폴더 | 로컬 디스크 `C:\wise`. 완성 후 Google Drive로 복사 |
| Q7 | 홈페이지 형태 | 독립 정적 사이트 |
| Q8 | 지도안 기존 초안 | `6. WISE_12차시_지도안.hwpx`를 `assets/base_지도안.hwpx`로 두고 서식 템플릿 겸 내용 기준으로 삼는다. 내용은 `lessons.json`이 정본이다 |

---

## 7. 실행 환경

- OS : Windows 11 / PowerShell 5.1 · Git Bash
- Python 3.12 (`python3` 또는 `py -3`) : `python-pptx`, `openpyxl`, `lxml`
- Node.js LTS : 웹앱·사이트 스모크 테스트
- 한글(HWP)이 설치되어 있지 않다. HWPX는 반드시 제자리 수정 방식으로 만들고 스크립트로 검증한다.

---

2026년 티처스랩 5기 교사연구회 A.N.D (Analog aNd Digital)
