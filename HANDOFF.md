# HANDOFF.md — 인수인계서

> 새 세션이나 다른 사람이 이 프로젝트를 이어받을 때 읽는 문서다.
> 마지막 갱신 : 2026-08-18

---

## 1. 지금 어디까지 되어 있나

| 항목 | 상태 |
|---|---|
| 92개 작업 | **전부 완료** |
| 자동 검사 | **87개 전부 통과** |
| 공개 사이트 | https://legoschool.github.io/ai-for-good/ |
| 저장소 | https://github.com/legoschool/ai-for-good (공개, 커밋 14개) |
| 팀 공유본 | 드라이브 `8. WISE 산출물` |
| Firebase 보안 규칙 | 적용 완료. 실제 요청 11번으로 확인함 |

### 만들어진 것

| 산출물 | 수량 |
|---|---|
| 교수·학습 지도안 | 12편 HWPX |
| 학생 활동지 | 12종 HWPX |
| 수업용 PPT | 12세트 PPTX |
| 수업 활용 웹앱 | 12개 + 공통 설문 1개 |
| 웹앱 구성안·제작 프롬프트 | 각 13편 |
| 통합 웹사이트 | 20쪽 |
| 학교자율시간 진도표 | 1종 XLSX |
| 카드 교구 | 3종 |
| 교사용 해설서 | 1종 |

전체 52MB.

---

## 2. 이어받는 법

```bash
cd C:\wise
py -3 build/verify_all.py        # 87개가 전부 OK 인지 먼저 본다
py -3 build/next_task.py --status
```

새 세션이면 `prompts/P0_오케스트레이터.md` 의 코드 블록을 첫 메시지로 붙여 넣는다.
`CLAUDE.md` 와 `STATE.md` 를 읽고 3줄로 상태를 보고한 뒤 시작한다.

**필요한 것** : Python 3.12(`python-pptx`, `openpyxl`, `lxml`), Node.js, GitHub CLI(`legoschool` 로그인 완료)

---

## 3. 남은 일

### 3-1. 3~12차시를 40분 수업으로 올리기 (가장 큰 일)

1·2차시만 40분 5단계 구조로 다시 만들었다. **3~12차시는 아직 기존 형태다.**

만드는 순서는 이렇다.

1. `data/lessons.json` 의 해당 차시 `webapp` 에 `steps` 5개를 넣는다. **시간 합이 40분이어야 한다.**
   검증기가 40분이 아니면 실패시킨다.
2. 활동이 단순하면 기존 5종(`buckets`, `form`, `vote`, `board`, `selfcheck`)을 그대로 쓴다.
   복잡하면 `build/webapp_l0N.py` 를 새로 만들고 `make_webapp.py` 의 `activity_for` 에 연결한다.
3. **걷기 검사를 함께 짠다.** `build/walkthrough.js` 에 그 차시를 추가한다.
   이것을 빼면 안 된다. 4-1절을 읽어라.
4. 지도안·활동지·PPT·사이트를 다시 만들고 `publish.py` 로 올린다.

3차시 제안 : 1차시 분류기를 재활용해 "데이터를 바꾸면 결과가 어떻게 달라지는가"로 잇는다.
1차시의 "못 본 것은 틀린다"가 3차시 편향으로 자연스럽게 이어진다.

### 3-2. 사람이 정해야 할 것

| 무엇 | 선택지 |
|---|---|
| **PPT 디자인** | 사이트·웹앱은 크림 바탕으로 통일했으나 PPT 12세트는 아직 옛 스타일이다. (가) 크림으로 통일 (나) 흰 바탕 유지하고 강조색만 가져오기. 프로젝터 가독성 때문에 (나)를 권한다 |

### 3-3. 사람이 해야 할 것

| 무엇 | 하는 법 |
|---|---|
| **시트 백업 연결** | `assets/AppsScript_Code.gs` 를 백업 시트의 Apps Script 에 붙이고 웹 앱으로 배포한다. 나온 주소로 `py -3 build/set_sheet_endpoint.py <주소>` 하면 13개 앱에 한 번에 들어간다. 지금은 Firebase 에만 쌓인다 |
| **시험 기록 지우기** | Firebase 콘솔 → 데이터 → `wise` → `signal-judges` → `999001`, `999002` 삭제. 규칙이 삭제를 막아 스크립트로는 못 지운다 |

---

## 4. 반드시 지킬 것

### 4-1. 구조 검사만으로는 부족하다

**실제로 겪은 일이다.** 1·2차시가 모든 구조 검사를 통과했는데도 둘 다 고장나 있었다.

- 1차시 : 4단계에서 데이터를 줄여도 정확도가 안 떨어졌다.
  이 차시의 핵심 장면이 **아예 일어나지 않았다.**
- 2차시 : 5개 문항 중 **4개의 정답이 뒤집혀 있었다.**
  학생이 환각을 정답으로 배울 뻔했다.

파일이 열리고 버튼이 눌린다고 수업이 되는 것이 아니다.
**차시를 만들 때 걷기 검사를 함께 짜라.**

```bash
node build/walkthrough.js L01
node build/walkthrough.js L02
```

학생처럼 처음부터 끝까지 돌려 보고 **핵심 장면이 실제로 일어나는지**를 판정한다.

### 4-2. 만든 것은 반드시 올린다

```bash
py -3 build/publish.py "무엇을 고쳤는지"
```

검증 → 커밋 → 올리기 → 사이트 재빌드 대기 → 실제 접속 확인 → 드라이브 사본까지 한 번에 한다.
검증이 실패하면 올리지 않는다. **돌리지 않았으면 아직 안 끝난 것이다.**

### 4-3. 산출물을 직접 고치지 않는다

`data/lessons.json` 이 단일 진실 원천이다. 산출물을 직접 고치면 12차시가 서로 어긋난다.

### 4-4. 파이썬 문자열이 JS 이스케이프를 삼킨다

`build/webapp_*.py` 의 `ACTIVITY` 안에서 JS 줄바꿈을 쓰려면 백슬래시를 **두 개** 적어야 한다.
하나만 적으면 파이썬이 먼저 진짜 줄바꿈으로 바꿔 JS 문법이 깨진다.
`smoke_webapp.js` 가 이것을 실제로 잡아냈다.

---

## 5. 이번 세션에서 정한 것

| 쟁점 | 결정 |
|---|---|
| 학생 식별 | 닉네임만. 실명·학번·연락처를 받지 않는다 |
| 웹앱 저장 | Firebase 실시간 DB(`/wise` 아래) + Google Sheets 백업 |
| 홈페이지 | 독립 정적 사이트. GitHub Pages |
| 디자인 | 커넥트재단 CONNECT ON: AI 포맷. 크림 바탕, 손그림 테두리, 형광 초록 |
| 차시 업그레이드 | 차시마다 40분 5단계 웹앱 + 단계 슬라이드 PPT |
| 학생 문장 수준 | 성취기준은 5·6학년군 유지, 문장은 3·4학년이 읽을 수준으로 |
| 배포 | `publish.py` 한 줄로 검증부터 접속 확인까지 |

### 학년에 대해

성취기준이 `[6실05-03]` 처럼 전부 **5·6학년군**이다. 학년을 낮추면 교육과정 근거가 사라진다.
문장만 3·4학년 수준으로 낮췄다. **읽기는 되지만 이해는 5·6학년 몫이다.**

---

## 6. 검사 87개가 무엇을 보는가

| 검사 | 수 | 무엇을 잡나 |
|---|---|---|
| `validate_data.py` | 1 | 필수 키, 단계 시간, 발문마다 예상 답변, 옛 용어 |
| `validate_hwpx.py` | 28 | 한글 보안 경고, 글자 겹침, 다른 차시 혼입 |
| `validate_pptx.py` | 12 | 폰트 미지정, 빈 발표자 노트, 교사 발문 누락 |
| `validate_webapp_spec.py` | 12 | 프롬프트에 지시 11종이 다 들어갔는지 |
| `smoke_webapp.js` | 14 | 외부 CDN, 역슬래시 정규식, 접근성, 안전 문구 |
| `run_webapp.js` | 14 | DOM 을 흉내 내 실제로 실행. 빈 제출 차단, 교사 집계 |
| `walkthrough.js` | 2 | **수업의 핵심 장면이 실제로 일어나는가** |
| `check_core_same.py` | 1 | 13개 앱의 공통 골격이 갈라지지 않았는지 |
| `check_reading.py` | 1 | 학생 문장 길이와 낱말 수준 |
| `check_rules.py` | 1 | Firebase 보안 규칙 |
| `validate_xlsx.py` | 1 | 진도표 시수 합계 13 |
| `check_site.js` | 1 | 끊긴 링크, 외부 참조 |
| `audit.py` | 1 | 개수, zip 무결성, 비밀값 잔존, 사본 일치 |

---

## 7. 파일이 어디에 있나

```
C:\wise
├─ CLAUDE.md          프로젝트 헌법. 가장 먼저 읽는다
├─ RUN.md             루프 돌리는 법
├─ STATE.md           92개 작업 보드. 결정 기록
├─ HANDOFF.md         이 문서
├─ data/lessons.json  단일 진실 원천
├─ spec/              규격 7종 + firebase_rules.json
├─ prompts/           도구별 실행 프롬프트 9종
├─ loops/             루프 정의 + 완료 정의 7종
├─ build/             생성 10종 + 검증 12종 + 루프 엔진 2종
├─ assets/            base_지도안.hwpx, AppsScript_Code.gs
└─ out/               산출물
```

---

2026년 티처스랩 5기 교사연구회 A.N.D · CC BY-NC-SA
