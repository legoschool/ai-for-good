# 인수인계서 · WISE 수업자료 개발

작성 2026-08-18 · 이 파일 하나만 읽으면 이어서 일할 수 있다.
새 창(새 세션)에서 병렬로 웹앱을 만들 때는 아래 5절을 그대로 따른다.

---

## 0. 먼저 열어 볼 것 (이것부터 본다)


**`★2026. 티처스랩 자료 개발/9. 2026. ai적정활용 티처스랩 개발.xlsx`**

개발 계획 원본이다. 이 파일을 먼저 꼼꼼히 읽고 나서 작업을 이어받는다.
`data/lessons.json` 은 이 엑셀을 옮겨 놓은 것이다. **둘이 어긋나면 엑셀이 맞는 것으로 보고
`lessons.json` 을 고친 뒤 전부 다시 만든다.**

| 시트 | 무엇이 들어 있나 | 어디에 쓰나 |
|---|---|---|
| `00_휴먼스킬 내용 체계` | 휴먼스킬 7영역의 정의와 지식·기능·태도 | 차시별 중점 스킬 배정의 근거 |
| `01_차시별_내용체계표` | 12차시 모듈·주제·학습문제·핵심활동·중점/보조 스킬 | 지도안·활동지·웹앱의 뼈대 |
| `02_산출물 계획` | 산출물 목록과 수량·분량 | 무엇을 몇 개 만들어야 하는지 |
| `03_AI적정활용` | 차시별 AI적정활용 구성요소와 실행 원칙 | 지도안의 적정활용 문구 |
| `04_휴먼스킬 7대 평가 체계` | 평가 관점·목표·상중하 기준·평가 방법 | 평가 기준표 |
| `05_평가` | 자기인식 설문 8문항(5점 리커트)과 연계 스킬 | 공통 설문 웹앱 |
| `06_운영결과` | **아직 비어 있다.** 9~11월 적용 뒤 채운다 | 운영 결과 기록 |

새로 만드는 산출물이 계획서(`2. 계획서.hwpx`)와 이 엑셀 중 어느 쪽과도 어긋나지 않는지
대조한 다음 넘어간다.

---

## 1. 지금 어디까지 왔나

2026-08-19 기준이다. **열두 차시 자료가 모두 만들어졌고 사이트에 올라가 있다.**

| 산출물 | 수 | 상태 |
|---|---|---|
| 웹앱 | 13개 (12차시 + 공통 설문) | 전부 여정형. 본문 21px, 학생 코드 네 자리, 시트 기록 |
| 교수·학습 지도안 | 한글 12편 + 인쇄용 HTML 12편 | 발문·예상답변, 활동별 웹앱 화면, QR, 캡처 3장 |
| 학생 활동지 | 한글 12종 + 인쇄용 HTML 12종 | 본문 16.5px, QR, 캡처 2장, 예시 상자 |
| 수업 PPT | pptx 12세트 (20~24장) | 웹앱 캡처 8장 + QR + 주소 |
| 수업 슬라이드 | HTML 12편 (차시당 30장) | 웹앱 사용법 9장이 전개 앞에 |
| 웹앱 사용 안내 | 13쪽 | 화면 캡처, 체험 3단계, 그 차시 자료 |
| 참고자료 모음 | 1쪽 | 26건. 1~12차시가 모두 찼다 |
| 관리자 대시보드 | 1식 | 시트에서 전체 학급, 사전·사후 변화, 영역별 변화 |
| 카드 교구 · 진도표 · 해설서 | 3종 · 1종 · 1종 | 그대로 |
| 자동 검사 | 83개 | 82개 통과. `audit.py` 만 드라이브 지연으로 시간 초과가 난다 |

**배포** https://legoschool.github.io/wise-ai/ (저장소 `legoschool/wise-ai`, `out/site` 를 그대로 올린다)

**만들어 둔 자산**

- 웹앱 화면 캡처 97장 (`out/site/assets/shots/`)
- 참고자료 누리집 미리보기 14장 (`out/site/assets/refshots/`)
- 차시별 QR 15개 (`out/site/assets/qr/`, SVG 와 PNG)

**아직 안 해 본 것 하나.** 교사가 방을 만들고 학생 여러 명이 동시에 들어가는
실제 수업 왕복은 아직이다. 시험 방(4차시 `191499`, 설문 `553203`)으로만 확인했다.
시험 기록은 시트에서 지우면 된다.

---

## 2. 폴더

```
8. WISE 산출물/
  README.md              교사용 안내 (수업에 쓰는 법)
  HANDOVER.md            이 파일
  out/                   배포본. 사이트에 올라가는 것과 같다
  harness/
    CLAUDE.md            프로젝트 헌법. 규칙이 충돌하면 이 파일이 이긴다
    RUN.md               루프 돌리는 법
    STATE.md             작업 보드. 스크립트가 갱신한다
    data/lessons.json    12차시 내용·참고자료·자료 묶음의 단일 진실 원천
    data/sheet_endpoint.txt  Apps Script 배포 주소 한 줄
    data/admin_code.txt  관리자 암호 한 줄 (없으면 wise2026)
    spec/                규격과 설계서
    build/               생성기와 검사기
    build/apps_script/   시트에 붙여 넣는 Apps Script 와 붙이는 법
    out/                 빌드 결과 (여기서 만들어 배포본으로 복사)
```

**만드는 스크립트가 무엇을 만드나**

| 스크립트 | 만드는 것 |
|---|---|
| `make_webapp.py` | 웹앱 13개 (`webapp_core.py` 골격 + `webapp_lNN.py` 활동) |
| `make_shots.js` | 웹앱 화면 캡처 97장. 헤드리스 크롬으로 실제 앱을 눌러 가며 찍는다 |
| `make_qr.py` | 차시별 QR (SVG). PPT 용 PNG 는 `make_ppt.py` 가 그때그때 만든다 |
| `make_ppt.py` | 수업 PPTX 12세트. 캡처와 QR 을 그림으로 넣는다 |
| `make_lesson_hwpx.py` · `make_worksheet_hwpx.py` | 한글 지도안·활동지 |
| `make_view.py` | 수업 슬라이드, 인쇄용 지도안·활동지 |
| `make_guide.py` | 웹앱 사용 안내 13쪽 |
| `make_refs.py` · `make_refshots.js` | 참고자료 모음과 누리집 미리보기 |
| `make_admin.py` | 관리자 대시보드 |
| `make_site.py` | 위의 화면 자료를 모아 사이트 72쪽을 만든다 |

**검사기**

| 스크립트 | 무엇을 본다 |
|---|---|
| `validate_data.py` | lessons.json 의 필수 키와 용어 |
| `smoke_webapp.js` | 웹앱 결함 10종 (외부 참조, 역슬래시, 개인정보 칸 등) |
| `run_webapp.js` | DOM 흉내 위에서 실제로 실행. 빈 제출 차단과 정상 제출 |
| `check_core_same.py` | 13개 앱의 골격이 갈라지지 않았는지 |
| `check_site.js` | 사이트 링크·외부 참조·임베드 허용 목록 |
| `validate_hwpx.py` · `validate_pptx.py` | 한글·PPT 무결성과 규격 |
| `audit.py` | 산출물 개수, 비밀값 잔존, 배포본 일치, 금지 표기 |
| `verify_all.py` | 위 전부를 한 번에 (83개) |

---

## 3. 무엇을 어떻게 만드나

모든 산출물은 `data/lessons.json` 에서 나온다. **산출물을 직접 고치지 않는다.**

```bash
cd "G:/내 드라이브/02_사업/00_지딜/지딜 엔드 티처스랩/2026. 티처스랩/★2026. 티처스랩 자료 개발/8. WISE 산출물/harness"

py -3 build/validate_data.py          # 먼저 통과시킨다
py -3 build/make_webapp.py all        # 웹앱 13개
node build/make_shots.js              # 웹앱 화면 캡처 97장 (앱을 고쳤으면 반드시)
node build/make_refshots.js           # 참고자료 누리집 미리보기 (인터넷 필요)
py -3 build/make_ppt.py all           # PPT (캡처를 그림으로 넣는다)
py -3 build/make_lesson_hwpx.py all   # 한글 지도안
py -3 build/make_worksheet_hwpx.py all
py -3 build/make_docs.py all
py -3 build/make_site.py              # QR·슬라이드·인쇄물·안내·참고자료·관리자를 한 번에
py -3 build/verify_all.py             # 83개 검사
```

**순서를 지킨다.** 캡처를 다시 찍지 않고 PPT 를 만들면 옛 화면이 그대로 들어간다.
`make_site.py` 는 안에서 `make_qr` `make_view` `make_admin` `make_guide` `make_refs` 를 부른다.

배포본으로 복사한다. 드라이브에서는 `cp` 가 잠김에 걸릴 때가 있어 `robocopy` 가 안전하다.

```powershell
robocopy "harness\out\site" "out\site" /MIR /R:2 /W:2 /MT:4
robocopy "harness\out\webapp" "out\webapp" /MIR /R:2 /W:2 /MT:4
```

GitHub 반영은 임시 폴더에 클론해 두고 `out/site` 를 통째로 덮어쓴 뒤 밀면 된다.

```bash
git clone https://github.com/legoschool/wise-ai.git wise-site
# out/site 내용을 wise-site 에 덮어쓰고
git add -A && git commit -m "..." && git push
```

---

## 3-1. 입장과 개인정보 (틀에서 이미 정해진 것)


바꾸지 않는다. 13개 앱이 전부 같다.

- **교사가 방을 만들면 여섯 자리 번호가 나온다.** 학생에게 번호만 알려 준다.
- **학생은 방 번호와 닉네임만으로 들어온다.** 비밀번호를 묻지 않는다.
  비밀번호 네 자리는 교사 화면을 다시 열 때만 쓰고, 교사 화면 안에만 있다.
- 이름·학교·학년·반·전화번호·사진을 묻는 칸을 만들지 않는다. 모둠 칸은 비워도 된다.
- 입장 화면에 안내를 고정한다.
  "개인정보를 모으지 않아요. 이름, 사진, 친구 이야기 같은 개인정보는 넣지 않아요.
  학교, 학년, 반, 전화번호도 묻지 않습니다. 닉네임과 답만 저장합니다.
  모인 자료는 수업 프로그램을 더 좋게 만드는 데만 쓰고, 학년도가 끝나면 지웁니다."
- 입장 화면에 **사전·사후 설문 안내**를 고정한다. 시작 전에는 사전, 12차시 뒤에는 사후.
- **상단바는 언제나 보인다.** 홈으로 돌아가기, 둘러보기, 차시 페이지, 사전·사후 설문, 처음부터.
  링크는 새 창으로 연다. 구글 사이트에 끼워 넣어도 화면이 갇히지 않는다.
- 설문 결과는 **전체 집계만** 보여 준다. 개인 응답을 교사 화면과 관리자 화면에 띄우지 않는다.

## 3-2. 관리자 화면


`out/site/admin.html` · 메뉴 맨 오른쪽 **관리자**.

- 암호는 `data/admin_code.txt` 한 줄. 파일이 없으면 `wise2026` 을 쓴다.
  암호를 바꾸면 `py -3 build/make_site.py` 를 다시 돌린다.
- 로그인 뒤 **차시와 방 번호를 등록**하면 그 방만 읽는다. 등록 목록은 그 브라우저에만 저장된다.
- 보여 주는 것 : 설문 문항별 사전·사후 평균과 변화량, 차시별 제출 인원, 전체 CSV.
- **이 암호는 가벼운 잠금이다.** 진짜 보호는 방 번호와 Firebase 규칙이 한다.
  방 번호를 모르면 아무 자료도 읽히지 않는다. 방 번호를 학급 밖으로 알리지 않는다.

## 4. 절대 규칙 (어기면 다시 만들어야 한다)


- `data/lessons.json` 이 원천이다. 지도안·활동지·PPT·웹앱·사이트가 전부 여기서 나온다.
- **HWPX 는 새로 조립하지 않는다.** `assets/base_지도안.hwpx` 를 복사해 제자리 수정한다.
  표 자체를 복제하면 셀 참조가 어긋나 한글이 보안 경고를 띄운다. 셀 **안에서만** 문단을 복제한다.
- 웹앱은 **단일 HTML**. 외부 CDN·글꼴·이미지를 부르지 않는다. 학교망에서 막힌다.
- 정규식에 `\d` `\s` `\w` 를 쓰지 않는다. Apps Script 가 역슬래시를 깨뜨린다. `[0-9]` 로 쓴다.
- 자바스크립트는 ES5 로 쓴다. 화살표 함수와 템플릿 문자열을 쓰지 않는다.
- em dash 를 쓰지 않는다. 신호등은 4단계다. 옛 3단계 표기를 쓰지 않는다.
- 학생 개인정보는 닉네임만 받는다. 실명·학교·학년·반·전화번호·사진 입력 칸을 만들지 않는다.
- 학생에게 비밀번호를 묻지 않는다. 방 번호와 닉네임이면 들어온다.
- 10차시 자가 점검은 익명 집계만 남긴다. 개인 응답을 교사 화면에 띄우지 않는다.
- 검사를 우회하지 않는다. 수리 3회 안에 못 고치면 멈추고 사람에게 묻는다.
- 학생이 보는 글자를 줄이지 않는다. 웹앱 본문 21px, 활동지 본문 16.5px 이 바닥이다.
- 한글(HWPX)에 그림을 새로 끼우지 않는다. 보안 경고가 난다. QR 과 캡처는 인쇄용 HTML 에 넣는다.
- 사이트에서 바깥을 부를 수 있는 곳은 `check_site.js` 의 `EMBED_OK` 에 적힌 곳뿐이다.
  임베드를 넣으면 같은 자료를 여는 링크도 반드시 함께 둔다. 학교망에서 막히는 곳이 있다.
- 참고자료는 **접속해서 확인한 것만** 넣는다. 확인하지 못한 주소를 목록에 올리지 않는다.

---

## 5. 새 창에서 웹앱을 하나 만들 때 (병렬 작업 절차)


**한 세션이 웹앱 한 차시를 맡는다.** 서로 다른 차시를 맡으면 파일이 겹치지 않는다.

### 5-1. 만지는 파일

| 맡은 차시 | 새로 쓰는 파일 | 건드리면 안 되는 파일 |
|---|---|---|
| N차시 | `build/webapp_lNN.py` 하나만 | `build/webapp_core.py`, `build/make_webapp.py`, 다른 차시 모듈 |

골격(`webapp_core.py`)을 고쳐야 할 일이 생기면 **먼저 알리고** 한 세션만 고친다.
골격이 갈라지면 `check_core_same.py` 가 잡아내고 13개 앱이 전부 어긋난다.

### 5-2. 설계부터 한다

`spec/10_웹앱_설계_L01.md` 를 형식 그대로 따라 `spec/1N_웹앱_설계_LNN.md` 를 먼저 쓴다.
5차시(`spec/15_웹앱_설계_L05.md`) · 6차시(`spec/14_웹앱_설계_L06.md`) · 12차시(`spec/16_웹앱_설계_L12.md`) 설계서가 최근 예다.
반드시 담을 것.

1. 이 앱이 만들어야 하는 **단 하나의 경험** (학생이 무엇을 저지르고 무엇을 겪는가)
2. 세계관과 캐릭터
3. 화면 지도 (허브 + 활동 화면 5~7개 + 결과 카드)
4. 화면별 설계 (학생이 하는 일, 보이는 것, 분 배분)
5. 저장하는 payload 모양
6. 교사 화면과 발표 모드에 무엇이 뜨는가
7. 활동지·PPT 와의 1:1 연결
8. 기기 없는 대안, 접근성, 안전
9. 완료 정의 체크리스트

색과 조각은 새로 만들지 않는다. **8절 디자인 규격을 그대로 쓴다.**
설계서에 "8절을 따른다" 한 줄을 적고, 벗어나는 것이 있으면 그것만 따로 적어 승인받는다.

설계서를 사람이 통과시킨 뒤에 코드를 쓴다.

### 5-3. 골격이 이미 주는 것 (다시 만들지 말 것)

방 만들기(6자리 코드)·입장·둘러보기·교사 대시보드·CSV·방 잠그기·발표 모드·
Firebase 저장·시트 백업·자동 임시 저장·상단바.

활동 코드에서 바로 부를 수 있는 것.

```
wiseGo(화면이름)        .quest 화면 전환
wiseHud([{label,done,total}...])   상단 진행 표시
wiseToast(글)          잠깐 뜨는 알림
wiseCelebrate(제목, [줄...], 버튼)  완료 축하 창
wiseCardPng(제목, [줄...], 파일이름) 결과 카드 PNG 저장
wiseBars([{label,value,color}...], 폭)  캔버스 막대 그림 (없으면 표로 대체)
wiseIcon(종류, 크기)    아이콘 SVG (id loc rel rec pub green yellow orange red me ai both write check again talk moon heart star)
wiseScene(종류)        설명 그림 (axis steps3 signal room)
dbGet(경로) / dbPush(경로, 값)   방 데이터 읽기 쓰기
esc / $ / pct / barHtml
```

활동이 정의해야 하는 것.

```
activityHtml()      화면 전체 HTML. 여정형이면 <section class="quest" data-q="이름"> 로 나눈다
activityInit(saved) 버튼 연결과 첫 화면
activityCollect()   제출값. 못 낼 상태면 null 을 주고 w-msg 에 까닭을 쓴다
teacherSummary(list) 교사 화면
activityEnter(id)   (선택) 화면에 들어올 때
activityDraft()     (선택) 자동 저장할 중간값
presentHtml(list)   (선택) 발표 모드
activityAutofill()  (선택) 자동 검사용 채우기. 여정형은 반드시 만든다
```

### 5-4. 만들고 나서 반드시

```bash
py -3 build/make_webapp.py N
node build/smoke_webapp.js out/webapp/LNN     # 결함 10종 검사
node build/run_webapp.js out/webapp/LNN       # DOM 흉내 위에서 실제 실행
py -3 build/check_core_same.py                # 골격이 갈라지지 않았는지
py -3 build/verify_all.py                     # 전체 83개
```

브라우저에서도 실제로 눌러 본다. 혼자 체험으로 끝까지 가 보고, 교사 화면에서 방을 만들어 본다.

### 5-5. 다 되면

`py -3 build/update_state.py "LNN/웹앱 구현" 완료 "여정형으로 다시 만듦"` 으로 기록하고,
사이트를 다시 만들어(`make_site.py`) 배포본에 복사한다.

---


### 5-6. 앱을 고쳤으면 캡처부터 다시 찍는다

캡처는 PPT, 인쇄용 지도안, 활동지, 사용 안내 네 곳에 들어간다.
`node build/make_shots.js` 를 건너뛰면 네 곳이 모두 옛 화면으로 남는다. 아무 검사도 잡아 주지 않는다.

```bash
py -3 build/make_webapp.py N     # 앱을 고치고
node build/make_shots.js         # 캡처를 다시 찍고
py -3 build/make_ppt.py all      # PPT 를 다시 만들고
py -3 build/make_site.py         # 나머지를 한 번에
```

---

## 6. 남은 일 (우선순위)

1. **실제 수업 왕복 테스트.** 교사가 방을 만들고 학생 여러 명이 동시에 제출하는 상황을 한 번 돌린다.
   끝나면 시험 데이터(방 `191499`, `553203`)를 시트와 Firebase 에서 지운다.
2. **사후 설문.** 12차시 뒤에 열린다. 지금은 사전 자료만 쌓인다.
   학급 인원을 시트 `방목록` 의 비고 칸에 적어 두면 참여율을 정확히 낼 수 있다.
3. **임베드 채우기.** 유튜브·캔바 임베드를 넣을 자리를 열어 두었다.
   `references` 항목에 `"embed"` 를 적으면 안내 페이지 안에서 재생된다. 지금은 링크만 있다.
4. **교사용 해설서 보강.** 계획 20쪽, 현재 6,000자.
5. **운영 결과 기록.** 개발 계획 엑셀 `06_운영결과` `07_설문결과` 시트를 9~11월 적용 뒤 채운다.
6. **환경부 국가환경교육센터 미리보기.** 접속이 느려 캡처를 못 떴다. `make_refshots.js` 를 다시 돌리면 채워진다.

---

## 7. 사람이 정해 준 것 (임의로 바꾸지 않는다)


| 쟁점 | 결정 |
|---|---|
| 학생 식별 | 닉네임만. 실명·학번·연락처 수집 안 함 |
| 8차시 이미지 업로드 | 기능 없음. 인쇄물이나 교사 화면 공유로 대체 |
| 10차시 위험 신호 | 익명 집계만 |
| 저장 | Firebase `remind-c2610` `/wise` + Google Sheets 백업 |
| 배포 | GitHub Pages 공개 저장소 `legoschool/wise-ai` |
| 1차시 캐릭터 | 몽이. 학급 라벨 비교는 실시간. 편식 실험은 슬라이더 |
| 1차시 카드 그림 | 실제 사진. CC0·퍼블릭 도메인만. 사진 안에 정답(품종 이름)을 적지 않는다 |
| 웹앱 방향 | 폼이 아니라 여정형. 바만사(사회정서교육) 앱을 기준으로 삼는다 |
| 입장 | 방 번호 + 닉네임만. 학생 비밀번호 없음 |
| 관리자 | 암호 로그인 화면을 두고 학급 집계만 본다. 개인 응답은 어디에도 띄우지 않는다 |

---

---

## 8. 디자인 규격 (새로 만드는 것도 이대로 맞춘다)


바탕은 **티처스랩 휴먼스킬 안내 화면**이다. 참고 그림은
`★2026. 티처스랩 자료 개발/2026. 티처스랩/티처스랩 교육자료 캡처/` 에 있다.
크림 바탕, 굵은 검정 테두리, 둥근 카드, 형광 초록 강조, 큰 제목.
규격 문서는 `harness/spec/05_디자인_시스템.md` 이고, 이 절이 실제로 쓰는 값이다.

### 8-1. 사이트·슬라이드·인쇄물 색 (`build/site_css.py`, `build/make_view.py`)

| 이름 | 값 | 쓰는 곳 |
|---|---|---|
| cream | `#F4EEE0` | 바탕 |
| cream-d | `#EBE3D2` | 눌린 구역, 인쇄물 바깥 |
| paper | `#FFFFFF` | 카드 |
| ink | `#101010` | 제목 |
| ink2 | `#33312D` | 본문 |
| muted | `#6F6A61` | 보조 설명 |
| line | `#111111` | 모든 테두리 |
| green | `#00D45A` | 강조, 주 버튼 |
| blue | `#2B59E0` | 링크, 파랑 강조 |
| purple | `#7B4FE8` | 태그 |
| sun | `#FFE24B` | 배지, 눌림 상태 |

- 테두리는 **2.5px 검정**, 카드 그림자는 `6px 6px 0 검정` (번짐 없는 딱딱한 그림자).
- 제목은 900 weight, `letter-spacing:-.028em`. 본문 17px, 줄간 1.72.
- 글꼴은 `Pretendard, Malgun Gothic, Apple SD Gothic Neo, Noto Sans KR, system-ui` 순.
  **웹 글꼴을 내려받지 않는다.** 학교망에서 막힌다.
- **다크 모드를 만들지 않는다.** 교실 프로젝터에서 어두운 화면은 보이지 않는다.

### 8-2. 웹앱 색 (`build/webapp_core.py`)

**웹앱도 사이트와 같은 규격이다.** 크림 바탕, 검정 2.5~3px 테두리,
번지지 않는 그림자 `5px 5px 0`, 900 굵기 제목. 8-1 표의 색을 그대로 쓴다.
버튼은 형광 초록 `#00D45A`, 누르면 2px 내려가고 그림자가 줄어든다. hover 는 노랑 `#FFE24B`.
모듈 색은 **머리말과 고른 항목**에만 쓴다.

| 이름 | 값 |
|---|---|
| ink | `#101010` |
| muted | `#6F6A61` |
| line | `#111111` |
| paper | `#FFFFFF` |
| bg | `#F4EEE0` (크림) |
| bg-d | `#EBE3D2` |
| shadow | `5px 5px 0 var(--line)` |

강조색은 차시가 속한 모듈에서 온다. **직접 정하지 않는다.**

| 모듈 | accent | accent-soft |
|---|---|---|
| 모듈1 발견 (1~4차시) | `#2563eb` | `#eff6ff` |
| 모듈2 판단 (5~8차시) | `#d97706` | `#fffbeb` |
| 모듈3 실천 (9~12차시) | `#059669` | `#ecfdf5` |
| 공통 설문 | `#1d4ed8` | `#eff6ff` |

신호등 네 색은 어디서나 같다. 초록 `#16a34a` · 노랑 `#eab308` · 주황 `#ea580c` · 빨강 `#dc2626`.

### 8-3. 웹앱에서 쓰는 조각 (있는 것을 쓰고 새로 만들지 않는다)

| 클래스 | 무엇 |
|---|---|
| `.card` | 흰 카드. 반지름 16, 테두리 1px, 그림자 |
| `.chip` | 세로로 쌓이는 선택 버튼. 최소 높이 48px. 고르면 `.on` |
| `.tile` | 허브의 큰 타일. 마치면 `.done` 이 붙어 강조색으로 바뀐다 |
| `.quest` | 여정 화면 한 장. `data-q="이름"` 으로 구분하고 `wiseGo()` 로 전환 |
| `.hud` | 상단 진행 표시. `wiseHud()` 가 채운다 |
| `.pill` `.tag` | 작은 배지 |
| `.bar` | 진행 막대. `barHtml(a, b)` |
| `.note` | 강조색 안내 상자 |
| `.safe` | 주황 안전 상자. 개인정보·마음 안내에만 쓴다 |
| `.wtop` | 상시 상단바 |
| `.cele` `.toast` | 완료 축하, 잠깐 알림 |
| `.spin` `.loading` | 도는 표시와 그 옆 글 한 줄. `wiseSpinner()` 가 만든다 |
| `.skel` | 회색 뼈대 자리. `wiseSkeleton(줄수)` 가 만든다 |
| `.busy-wrap` | 화면 전체 대기 덮개. `wiseBusy(on, 글)` 이 만든다 |
| `.fade-in` | 다시 그린 자리를 0.28초 동안 떠오르게 한다 |
| `.g2` | 두 칸 격자. 좁은 화면에서 한 칸으로 접힌다 |

버튼은 세 종류뿐이다. 기본(강조색 채움) · `.ghost`(흰 배경 강조색 글씨) · `.plain`(회색 테두리).

### 8-4. 그림

- **전부 인라인 SVG.** 이미지 파일, 아이콘 글꼴, 외부 그림을 쓰지 않는다.
- 사이트 그림은 `build/site_art.py`, 웹앱 그림은 `build/webapp_art.py` 에 모은다.
  같은 그림을 두 곳에 복사하지 않는다.
- 선은 검정 `#111111`, 굵기 3, 모서리는 둥글게(`stroke-linejoin="round"`). 면은 단색이다.
  그러데이션과 그림자를 SVG 안에 넣지 않는다.
- 팔레트 : 초록 `#00D45A` · 파랑 `#2B59E0` · 보라 `#7B4FE8` · 노랑 `#FFE24B` ·
  크림 `#F4EEE0` · 빨강 `#FF6B5A` · 흰색 · 검정.
- 차시 대표 그림은 `viewBox="0 0 320 180"`, 아이콘은 `0 0 48 48`.
- 아이콘은 `wiseIcon(종류, 크기)` 로 부른다. 종류를 새로 만들려면 `webapp_art.py` 에 추가하고
  인수인계서 목록에도 적는다.

### 8-5. 글쓰기와 접근성

- 학생 문장은 초등 5·6학년이 읽는 문장이다. 한 문장 40자 이내를 기준으로 한다.
  존댓말 청유형(`~해 봐요`, `~인가요`)을 쓴다.
- 교사용 문장은 평서형(`~한다`)으로 쓴다.
- **em dash 를 쓰지 않는다.** 쉼표와 마침표로 푼다. 검사기가 잡는다.
- 부호 약속 : `☆` 교사 준비물 · `★` 학생 배부물과 웹앱 · `△` 지도 유의점.
- 누르는 곳은 최소 48px, 본문은 16px 이상.
- **색만으로 뜻을 구분하지 않는다.** 신호등은 색과 글자를 함께 쓴다.
- 움직임은 `prefers-reduced-motion` 을 존중한다. 1차시 학습 애니메이션이 그 예다.
- 표가 넓으면 `.scroll` 상자로 감싼다. 본문이 가로로 밀리면 안 된다.

### 8-6. 기다림과 움직임 (딱딱 넘어가지 않게)

화면이 갑자기 바뀌거나, 눌렀는데 아무 일도 없어 보이면 학생이 두 번 세 번 누른다.
아래를 지키면 그런 일이 없다.

- **화면 전환** : `.quest.on` 이 0.34초 동안 살짝 올라오며 나타난다. 직접 CSS 를 쓰지 말고
  `wiseGo()` 만 부른다. 전환은 골격이 한다.
- **네트워크를 기다릴 때** : 반드시 자리에 무엇을 채운다. 빈 화면을 두지 않는다.
  ```
  $("dist").innerHTML = wiseSpinner("우리 반 판정을 불러오는 중이에요");
  $("box").innerHTML  = wiseSpinner("불러오는 중", true) + wiseSkeleton(3);
  ```
- **버튼을 누른 직후** : `wiseButtonBusy(el, true, "보내는 중")` 으로 스피너를 넣고 잠근다.
  끝나면 `wiseButtonBusy(el, false)`. 제출 버튼과 방 만들기는 골격이 이미 한다.
- **화면 전체가 멈춰야 할 때** : `wiseBusy(true, "방을 만드는 중")` · `wiseBusy(false)`.
  방 만들기처럼 다른 조작을 막아야 할 때만 쓴다.
- **1초 이상 걸리는 계산** : 결과를 바로 뱉지 말고 짧은 애니메이션을 끼운다.
  1차시 학습 버튼이 1.2초 동안 공책이 채워지는 그림을 보여 주는 것이 그 예다.
- **`prefers-reduced-motion`** 을 켠 기기에서는 모든 애니메이션이 멈춘다.
  그래서 스피너 옆에는 **항상 글자를 함께 둔다.** 글자만으로도 상태를 알 수 있어야 한다.

골격이 주는 것.

```
wiseSpinner(글, 큰가)      스피너와 글 한 줄
wiseSkeleton(줄수)         회색 뼈대 자리 (표나 목록을 기다릴 때)
wiseBusy(켬끔, 글)         화면 전체 대기 덮개
wiseButtonBusy(버튼, 켬끔, 글)  버튼 안에 스피너를 넣고 잠근다
```

활동 코드에서 지킬 것.

- 허브 타일과 되돌아가기처럼 **화면을 갈아 끼울 때** `wiseBusy(true, "무엇을 하는 중")` 을 띄우고
  0.3~0.5초 뒤에 `wiseBusy(false)` 와 `wiseGo()` 를 부른다. 0.6초를 넘기지 않는다.
- 다음 문제, 다음 카드로 **자동으로 넘어갈 때**도 같은 기다림을 둔다. 곧바로 갈리면 학생이 놓친다.
- 기다림 문구는 세계관 말로 쓴다. "장면 카드를 꺼내는 중"처럼 무엇을 하는 중인지 밝힌다.
- 화면 안에서 조각만 갈아 끼울 때는 새로 그린 상자에 `class="fade-in"` 을 준다.
- `window.matchMedia` 가 없는 자리(검사기)에서는 기다림을 건너뛰고 곧바로 넘어가게 만든다.
  검사가 멈추면 안 된다.
- **화면에 들어올 때**는 뼈대를 먼저 깔고 0.2초쯤 뒤에 진짜 내용을 그린다. 튀어나오지 않고 올라온다.
  ```
  $("stage6").innerHTML = wiseSpinner("상황 카드를 꺼내는 중이에요") + wiseSkeleton(3);
  softDelay(paint, 200);
  ```
- **고른 것을 기록했다고 먼저 알린다.** 자동으로 다음 카드로 넘어가는 화면은
  "기록했어요. 다음 카드로 넘어가요." 를 0.5초 보여 준 뒤에 넘긴다.
- **임시 저장을 되살렸으면 말해 준다.** `wiseToast("지난번에 판정하던 것이 남아 있어요.")`
  학생이 자기 기록이 사라졌다고 오해하지 않게 한다.
- 뜸 들이기는 활동 안에 `softDelay(fn, ms)` 한 개만 둔다. `prefers-reduced-motion` 이면 곧바로 부른다.
  네트워크를 기다리는 자리에는 뜸을 들이지 않는다. 기다림이 필요한 것은 사람의 눈이지 자료가 아니다.
- 5차시(`build/webapp_l05.py`)와 6차시(`build/webapp_l06.py`)가 이 규격대로 되어 있다.
  여정형으로 옮길 때 그 코드를 본뜬다.
- 3차시(`build/webapp_l03.py`)는 기다림을 활동마다 다르게 두었다. 그림 뽑기 0.9초,
  창고를 바꾸어 다시 뽑기 1.1초, 세어 본 수 맞춰 보기 0.6초, 수첩 카드 만들기 0.5초.
  기다리는 동안 `wiseSpinner(글, 큰가) + wiseSkeleton(3)` 을 자리에 깔고 버튼을 잠근다.
  `prefers-reduced-motion` 을 켠 기기에서는 기다리지 않고 곧바로 결과를 보여 준다.

### 8-7. 화면 짜임 (여정형 앱은 이 순서를 지킨다)

```
입장(방 번호 + 닉네임) → 이야기 한 장 → 허브(타일 5~6개, 진행, 배지)
 → 활동 화면 3~5개 → 결과 카드(PNG 저장) → 제출과 완료 축하
```

- 허브가 중심이다. 어느 화면에서나 **판정소로 / 교무실로** 같은 버튼으로 허브에 돌아온다.
- 상단바는 늘 보인다. 첫 항목은 **홈으로 돌아가기** 다.
- 활동 화면 하나에 한 가지 일만 둔다. 폼을 길게 늘어놓지 않는다.
- 마지막에는 반드시 손에 남는 것이 있다. 결과 카드 PNG 또는 배지.

---

## 9. 알아 두면 시간을 아끼는 것


- **우리말 글을 견줄 때는 낱말 전체로 세지 않는다.** 조사와 어미가 붙어 모양이 바뀐다.
  9차시는 앞 두 글자(어간)로 견준다. 낱말 전체로 세면 잘 고쳐 쓴 글이 10%대로 나와
  "처음 생각이 사라졌다"는 거짓 경고가 뜬다. 문턱은 80% 위와 10% 아래다.
- **백업 시트 ID 를 공개 자료에 넣지 않는다.** 학생 기록이 쌓이는 시트다. PROMPT.md 에서 뺐다.
- **사이트 검사는 바깥 링크(a href)만 허용한다.** script·link·img 로 외부를 부르면 검사가 막는다.
- **1차시 카드 더미는 실측으로 고른 것이다.** 귀 하나로 갈리는 더미를 쓰면 편식 실험이 무너지지 않는다.
  카드를 바꾸려면 정확도 곡선(전체 → 8 → 6 → 4 → 2장)을 다시 재고 설계서 표를 갱신한다.
  지금 값은 사진대로 붙였을 때 100 → 100 → 67 → 50 → 33점, 귀만 보고 붙였을 때 83점 고정이다.
- **1차시 사진은 CC0·퍼블릭 도메인만 쓴다.** 원본은 `assets/pets/*.jpg`, 출처는 같은 폴더 `credits.json`.
  사진을 바꾸면 `credits.json` 을 함께 고치고 앱을 다시 만든다. 앱이 빌드할 때 사진을 읽어
  data URI 로 넣기 때문에, 파일이 없으면 빌드가 실패한다.
- **사진에서 보이지 않는 특징은 카드에 적지 않는다.** 꼬리가 안 보이는 사진은 쓰지 않았다.
  네 가지 특징은 귀 모양·꼬리 길이·몸집·**털 색**이다. 코 색은 사진에서 잘 보이지 않아 뺐다.
- **웹앱 상단바는 따로 열었을 때만 보인다.** 구글 사이트에 끼워 넣으면 `window.self !== window.top` 이라 숨는다.
- 로컬에서 사이트를 보려면 `8. WISE 산출물` 에서 `py -3 -m http.server 8765`.

---

2026년 G-DEAL A.N.D · CC BY-NC-SA
## 10. 변경 이력 (날짜순)

만들면서 걸렸던 것과 그 까닭을 남긴다. 같은 곳에서 두 번 넘어지지 않게 하려는 것이다.

---

### 10-1. 2026-08-18 저녁에 고친 것 (이어받는 사람이 알아야 하는 변경)


| 무엇 | 왜 | 어디 |
|---|---|---|
| 1차시 카드를 **실제 사진**으로 바꿈 | 그린 그림으로는 "진짜"가 아니라는 지적 | `assets/pets/`, `build/webapp_l01.py` |
| 네 번째 특징을 코 색 → **털 색**으로 바꿈 | 사진에서 코는 잘 안 보인다. 털 색은 양쪽에 고르게 퍼져 편식 실험이 산다 | `build/webapp_l01.py`, `spec/10` |
| 교시마다 **다음 교시 버튼** 추가 | 2교시에서 3교시로 넘어가지 못했다 | `build/webapp_l01.py` |
| 글자 크기를 **16px → 19px** 로 올림 (모바일 18px, 입력 20px, 버튼 52px) | 교실에서 글자가 작다 | `build/webapp_core.py`, `spec/05` |
| 학습·시험·편식에 **기다리는 표시**를 넣음 | 결과가 딱딱 튀어나와 무슨 일이 있었는지 안 보인다 | `build/webapp_l01.py` |
| 6차시 설계서를 뒤늦게 씀 | 앱은 있는데 설계서가 없었다. 고칠 근거가 없다 | `spec/14_웹앱_설계_L06.md` |
| 6차시 **조건 입력이 사라지던 것**을 고침 | 판정을 바꾸면 조건 칸을 다시 그리면서 쓴 글이 지워졌다 | `build/webapp_l06.py` |
| 6차시 **신호·근거 순서에 상관없이** 다음 카드로 넘어가게 함 | 근거를 먼저 고르면 넘어가지 않았다 | `build/webapp_l06.py` |
| 6차시에 **기다리는 표시와 뜸 들이기**를 넣음 | 화면이 딱딱 넘어간다는 지적 | `build/webapp_l06.py`, 8-6절 |
| 12차시를 **여정형으로 다시 만듦** | 폼 한 장이라 발표와 성찰이 한 화면에 뭉쳐 있었다 | `build/webapp_l12.py`, `spec/16_웹앱_설계_L12.md` |
| 12차시에 **표기표**를 넣음 | AI를 어디까지 썼는지 학생이 스스로 문장으로 만들게 한다 | `build/webapp_l12.py` |
| 12차시 **쓴 글을 상태에 담음** | 나갔다 들어오면 글이 비었다. 6차시 사고와 같은 종류 | `build/webapp_l12.py` |

**기다리는 표시 규칙** (다른 차시를 여정형으로 고칠 때도 그대로 쓴다)

- 계산이 순식간에 끝나도 **일부러 1초쯤 기다린다.** 무엇을 하는 중인지 글로 알려 준다.
  학습 1.3초, 채점 0.9초, 편식 재학습 1.1초.
- 골격이 이미 주는 것을 쓴다. 새로 만들지 않는다.
  `wiseSpinner(글, 크게)` 도는 표시 · `wiseSkeleton(줄수)` 뼈대 · `wiseButtonBusy(버튼, on, 글)` 버튼 잠금 ·
  `wiseBusy(on, 글)` 화면 전체 덮기.
- 결과는 한꺼번에 쏟지 않고 **한 장씩 차례로** 올린다(카드당 0.1초). 골격의 `wfade` 를 그대로 쓴다.
- **움직임을 줄이도록 설정한 기기에서는 기다리지 않는다.** `prefers-reduced-motion` 을 보고 0초로 만든다.
  검사(`run_webapp.js`)도 이 경로로 돈다.

**골격이 같은 시각에 두 번 고쳐졌다.** 22:04 에 다른 세션이 `webapp_core.py` 를 '티처스랩 결'
(크림 바탕·검정 테두리) 로 바꾸면서 이 세션이 올려 둔 글자 크기가 함께 지워졌다.
새 디자인 위에 글자 크기를 다시 얹어 두었다. **골격을 고칠 때는 반드시 먼저 알리고 한 세션만 고친다.**
고친 뒤에는 `make_webapp.py all` → `make_site.py` → `verify_all.py` 를 이 순서로 돌린다.

글자 크기는 **골격(`webapp_core.py`)** 을 고친 것이라 13개 앱에 모두 적용된다.
그래서 `py -3 build/make_webapp.py all` 로 전부 다시 만들었다. 앞으로 골격을 고치면 같은 순서로 한다.
`smoke_webapp.js` 의 누르는 곳 높이 검사도 48px 고정 문자열에서 48px 이상으로 고쳤다.

---

### 10-2. 2026-08-18 밤에 고친 것 (3차시)


| 무엇 | 왜 | 어디 |
|---|---|---|
| 3차시를 **여정형으로 다시 만듦** | 폼 한 장으로는 편향을 겪지 못한다 | `build/webapp_l03.py`, `spec/12_웹앱_설계_L03.md` |
| 뽑기를 무작위에서 **몫 나누기**로 바꿈 | 무작위면 세어 보기가 매번 흔들려 비율을 바꾼 효과가 묻힌다. 82% 면 열두 장 중 열 장이 남성이다 | `build/webapp_l03.py` |
| **사진 창고** 화면을 넣음 | 이 차시의 심장이다. 그리는 방법은 그대로 두고 창고 비율만 바꿔 결과가 달라지는 것을 학생이 직접 만든다 | 같은 파일 |
| 짐작 → 세어 보기 → 실제를 **한 줄에 견줌** | 짐작이 있어야 놀란다. 맞고 틀림은 매기지 않는다 | 같은 파일 |
| 기다리는 표시를 네 곳에 넣음 | 결과가 딱딱 튀어나와 무슨 일이 있었는지 안 보인다 | 같은 파일, 8-6 절 |
| 제출이 막히면 **그 화면으로 데려감** | 무엇이 모자란지 글로만 알려 주면 학생이 못 찾는다 | 같은 파일 |

3차시 앱의 단 하나의 경험은 **학생이 편견을 직접 만들고 직접 없애는 것**이다.
공장을 나무라는 화면을 만들지 않는다. 고칠 곳은 창고다.

---

### 10-3. 2026-08-18 밤에 고친 것 (1~6차시 통합 점검)


| 무엇 | 왜 | 어디 |
|---|---|---|
| 활동 화면마다 **다음 화면 버튼을 골격이 자동으로 붙임** | 2·4·5·6차시도 화면 끝에서 막혀 있었다. 허브로 돌아가야만 다음으로 갈 수 있었다 | `build/webapp_core.py` (`addNextButtons`, `syncNextButton`) |
| 1~6차시를 브라우저에서 **입장 → 활동 → 제출까지 실제로 눌러 봄** | 자동 검사는 통과해도 학생이 못 끝내는 화면이 있었다 | 확인 기록만 |

**다음 화면 버튼 규칙** (차시 코드가 아무것도 하지 않아도 된다)

- 골격이 `activityInit` 뒤에 화면을 훑어 **'다음 : 다음 화면 제목'** 버튼을 붙인다.
  이야기·허브·마지막 화면에는 붙이지 않는다.
- 차시가 이미 `id="...-go"` 버튼이나 '…로 가기' 버튼을 두었으면 **자동 버튼은 숨는다.**
  화면에 들어올 때마다 다시 판단하므로 늦게 만들어지는 버튼과도 겹치지 않는다.
- 앞으로 가는 버튼으로 세는 것 : `id` 가 `-go` 로 끝나거나 '…로 가기 / 보러 가기 / 하러 가기'.
  '다음 카드', '다음 문항', '돌아가기' 는 같은 화면 안 이동이라 세지 않는다.
- 화면 순서는 `activityHtml()` 이 만든 **DOM 차례**를 그대로 따른다.

**점검하는 법** (7~12차시를 고친 뒤에도 이대로 한다)

1. `py -3 build/make_webapp.py N` → `node build/smoke_webapp.js out/webapp/LNN` → `node build/run_webapp.js out/webapp/LNN`
2. 브라우저에서 닉네임만 넣고 **둘러보기**로 들어가 활동을 끝까지 하고 제출까지 눌러 본다.
   제출이 막히면 막은 문구가 무엇을 하라고 하는지 읽고 그대로 해 본다.
3. 화면마다 앞으로 가는 길이 **하나만** 있는지 본다. 없으면 막힌 것이고, 둘이면 겹친 것이다.

---

### 10-4. 2026-08-19 새벽에 만든 것 (7차시)


| 무엇 | 왜 | 어디 |
|---|---|---|
| 7차시를 **여정형으로 다시 만듦** | 폼 한 장으로는 '금지가 왜 약한지'를 겪지 못한다 | `build/webapp_l07.py`, `spec/16_웹앱_설계_L07.md` |
| **상황 검사기** 를 심장으로 둠 | 학생이 쓴 금지형 문장을 교실 상황 세 개에 넣어 보고 막히는 것을 눈으로 본다 | 같은 파일 |
| 고치기 전후 문장을 나란히 남김 | 고쳤다는 사실이 기록으로 남아야 배지와 교사 화면이 산다 | 같은 파일 |
| 혼자 체험용 **예시 제안 8개** | 방이 없어도 투표와 8조항 확정을 끝까지 해 볼 수 있다 | 같은 파일 |
| 목록을 지우지 않는 기다림 표시 | 목록을 통째로 스피너로 바꾸면 겹쳐 눌렀을 때 화면이 멈춘 것처럼 보인다 | 같은 파일 `busyBox` |

**7차시 앱의 단 하나의 경험은 '내가 쓴 금지 문장이 상황에서 막히는 것'이다.**
또박이는 옳고 그름을 말하지 않는다. 상황에 넣어 보고 답이 나오는지만 보여 준다.
한 조항이 세 상황을 모두 다루지 못하는 것도 그대로 보여 준다. 그래서 여덟 조항이 필요하다.

**만들 때 걸렸던 것** : 목록을 `innerHTML = wiseSpinner(...)` 로 통째로 바꾸면,
학생이 빠르게 두 번 누를 때 다시 그리기 전에 다음 클릭이 사라져 화면이 멈춘 것처럼 보인다.
목록은 남겨 두고 **흐리게(opacity) + 한 줄 알림**으로 바꾸고 `st.busy` 로 겹쳐 누르기를 막았다.
다른 차시에서 목록을 다시 그릴 때도 이 방법을 쓴다.

---

### 10-5. 2026-08-19 아침에 고친 것 (9차시)


| 무엇 | 왜 | 어디 |
|---|---|---|
| 9차시를 **여정형으로 다시 만듦** | 세 단계를 폼 세 칸으로 두면 순서가 지켜지지 않는다 | `build/webapp_l09.py`, `spec/18_웹앱_설계_L09.md` |
| **자물쇠 세 개**로 순서를 강제함 | 다섯 문장을 넘겨야 질문이, 질문을 지어야 기록이, 기록을 남겨야 다시 쓰기가 열린다 | 같은 파일 |
| 잠긴 타일을 누르면 **열어야 할 화면으로 데려감** | 막기만 하면 학생이 무엇을 해야 할지 모른다 | 같은 파일 |
| "대신 써 줘"를 **곧바로 되짚음** | 지도 유의점이다. 되짚은 횟수는 교사 화면에 모인다 | 같은 파일 |
| 2단계를 **한 줄씩 나눠 적고** 줄마다 받아들임을 고르게 함 | AI 답을 통째로 붙이면 무엇을 받아들였는지 남지 않는다 | 같은 파일 |
| 겹침 계산을 **어간 두 글자 기준**으로 바꿈 | 낱말 전체로 세면 잘 고쳐 쓴 글이 11%로 나와 "처음 생각이 사라졌다"는 거짓 경고가 떴다 | 같은 파일 |

9차시 앱의 단 하나의 경험은 **AI 에게 던지는 말을 바꾸는 것**이다.
앱이 AI 를 부르지 않는다. 진짜 AI 는 교사 계정이나 모둠 공용 기기에서 쓰고, 앱은 그 과정을 기록한다.

---

### 10-6. 2026-08-19 아침 : 드라이브에서 지워졌다가 복원한 뒤 정리


**무슨 일이 있었나.** 8월 19일 06:10 에 `8. WISE 산출물` 아래 파일이 통째로 사라졌다.
여러 세션이 같은 폴더를 동시에 만지던 중이었다. 사람이 구글 드라이브 휴지통에서 복원했다.

**복원 부작용.** 삭제 뒤 빈 폴더 껍데기(`out`, `harness/out`, `out/site`)가 남아 있어서
복원본이 옆에 새 이름으로 들어왔다. 남아 있는 찌꺼기는 다음과 같다.

- `harness/out/site/webapp (1)` ~ `webapp (39)` : 같은 웹앱 폴더가 39벌
- `out/site (1)` `out/site (2)` `out/site (3)` : 배포본 사이트 사본 3벌
- `out/webapp (1)` (안에 `L09 (1)` 포함) · `out/webapp (2)`
- 복원된 배포본 `out/webapp/L10` 은 옛 폼형이었다. 아래 절차로 다시 만들어 바로잡았다.

**어떻게 되살렸나.** 소스(`build/`, `spec/`, `data/lessons.json`)는 온전히 복원되었다.
산출물을 손으로 고치지 않고 소스에서 전부 다시 만들었다. 규칙 그대로다.

```
py -3 build/validate_data.py      OK
py -3 build/make_webapp.py all    웹앱 13개 다시 생성
py -3 build/check_core_same.py    골격 해시 1종 (13개 동일)
py -3 build/make_site.py          사이트 58쪽
node build/smoke_webapp.js …      12차시 + 공통 전부 OK
node build/run_webapp.js …        12차시 + 공통 전부 OK
```

**지금 상태 (찌꺼기를 지우기 전).** `verify_all.py` 는 81개 통과, 2개가 **시간 초과**로 실패한다.
`check_site.js` 와 `audit.py` 가 찌꺼기 39벌까지 훑느라 180초를 넘긴다. 결함이 아니다.
찌꺼기를 뺀 사본으로 `check_site.js` 를 돌리면 `페이지 58개, 끊긴 링크 없음, 외부 참조 없음` 으로 통과한다.
찌꺼기를 지우면 두 검사도 그대로 통과한다.

**이어받는 사람이 할 일.**

1. 찌꺼기 폴더를 지운다. `harness/out/site/webapp (n)`, `out/site (n)`, `out/webapp (n)`.
   소스에서 다시 만들 수 있는 것뿐이라 지워도 잃는 것이 없다.
2. 배포본을 새로 맞춘다.
   `rm -rf out/webapp out/site && cp -r harness/out/webapp out/webapp && cp -r harness/out/site out/site`
3. `py -3 build/verify_all.py` 로 83개를 다시 확인한다.
4. GitHub(`legoschool/wise-ai`)에 `out/site` 를 올린다.

**교훈.** 한 폴더를 여러 세션이 동시에 만지면 골격이 덮어써지고, 최악의 경우 이렇게 지워진다.
차시별로 파일을 나눠 맡고, 골격과 배포 명령은 한 세션만 돌린다.

---

### 10-7. 2026-08-19 : 기록 설계와 관리자 대시보드


**왜 했나.** 사전·사후 설문이 쌓여도 이어 볼 방법이 없었고, 전체 관리자가 학급을 모아 보는 화면도 없었다.
효과성을 나중에 분석하려면 무엇을 어떤 모양으로 남길지 먼저 정해야 한다.

**사람이 정해 준 것 (2026-08-19)**

| 쟁점 | 결정 |
|---|---|
| 전체 학급 목록 | 구글 시트 `방목록` 탭에 남긴다. 관리자 화면이 시트에서 읽는다. Firebase 규칙은 건드리지 않는다 |
| 사전·사후 연결 | **학생이 정한 숫자 네 자리**를 쓴다. 실명·학번을 쓰지 않는다. 안 쓴 학생도 학급 평균에는 들어간다 |

**만든 것**

| 무엇 | 어디 |
|---|---|
| 기록 설계와 효과성 측정 문서 | `spec/20_기록과_효과성_측정.md` |
| 시트용 Apps Script (탭·머리글·집계·방목록) | `build/apps_script/wise_sheet.gs` |
| 붙여 넣고 배포하는 법 | `build/apps_script/README.md` |
| 입장 화면 학생 코드 네 자리 | `build/webapp_core.py` (13개 앱 공통) |
| 방을 만들면 시트 방목록에 기록 | `build/webapp_core.py` `sheetRoom()` |
| 설문 앱 안의 코드 칸과 안내 | `build/make_webapp.py` `survey_activity()` |
| 관리자 대시보드 (전체 학급 불러오기, 사전·사후 집계, 영역별 변화, 차시별 참여) | `build/make_admin.py` |

**시트 탭 구조**

```
방목록      시각 · 앱 · 차시 · 방코드 · 학급이름표 · 비고
설문        시각 · 방코드 · 학생코드 · 닉네임 · 사전사후 · 문항1~8 · 영역5 · 전체평균 · 자유응답2
제출_통합   시각 · 앱 · 차시 · 방코드 · 학생코드 · 닉네임 · 모둠 · 지표요약 · payload원문
L01~L12     시각 · 방코드 · 학생코드 · 닉네임 · (차시별 지표)
```

1번 문항은 시트에 넣을 때 **역채점**(6 - 값)한다. 화면과 시트가 같은 방향을 보게 한다.

**~~사람이 해야 하는 일 (아직 안 됨)~~ → 끝났다 (2026-08-20 확인).**
Apps Script 는 이미 새로 배포되어 있다. 지금 주소는 `방목록` 을 되돌려 주고
관리자 화면이 시트를 읽는다. 10-10 절의 점검표를 보라.
`build/apps_script/README.md` 의 1~5 단계를 따른다. 주소가 그대로면 하네스는 손볼 것이 없다.

**골격을 고쳤다.** 입장 화면에 칸이 하나 늘었으므로 13개 앱을 전부 다시 만들었다.
`make_webapp.py all` → `make_admin.py` → `make_site.py` → 검사 순서를 지킨다.

---

### 10-8. 2026-08-19 : 웹앱 캡처, QR, 사용 안내, 참고자료


**왜 했나.** 지도안만 보고는 웹앱을 어떻게 여는지 알 수 없었다. 교사가 화면을 못 찾으면 수업이 멈춘다.

**만든 것**

| 무엇 | 어디 | 수 |
|---|---|---|
| 웹앱 화면 캡처 (헤드리스 크롬) | `build/make_shots.js` → `out/site/assets/shots/` | 97장 |
| 차시별 QR (웹앱 주소) | `build/make_qr.py` → `out/site/assets/qr/` | 15개 |
| 웹앱 사용 안내 페이지 | `build/make_guide.py` → `out/site/guide/` | 13쪽 |
| 참고자료 모음 | `build/make_refs.py` → `out/site/refs.html` | 1쪽 |
| 참고자료 누리집 미리보기 | `build/make_refshots.js` → `out/site/assets/refshots/` | 14장 |

**어디에 붙었나**

- **수업 슬라이드** : 웹앱 사용법 아홉 장이 <b>전개 앞</b>에 들어간다. 도입을 마치고 앱을 켜는 자리다.
- **PPTX** : 같은 내용을 여덟 장 넣었다. 캡처와 QR 을 그림으로 붙인다. 차시당 20~25장이 되었다.
- **인쇄용 지도안·활동지** : 맨 위에 QR, 주소, 화면 캡처 세 장(활동지는 두 장)이 붙는다.
- **한글 지도안·활동지** : 주소와 사용 안내 쪽 주소를 글로 적었다.
  한글에는 그림을 새로 넣지 않는다. 넣으면 보안 경고가 난다. QR 은 인쇄용 HTML 에 있다.
- **차시 페이지** : 그 차시 자료를 먼저 보여 주고 공통 자료를 뒤에 붙인다.

**참고자료 26건.** 전부 접속해서 살아 있는지, 초등 수준인지 확인하고 넣었다.
1~12차시가 모두 찼다. 출처는 개인정보보호위원회, 교육부, KERIS, 경기도교육청,
디지털윤리.kr, 스마트쉼센터, 환경부 국가환경교육센터, 커넥트재단이다.

**임베드를 열었다.** `build/check_site.js` 의 `EMBED_OK` 에 적힌 곳만 iframe 으로 끼울 수 있다.
지금은 유튜브와 캔바다. 넣으려면 `references` 항목에 `"embed": "..."` 를 적는다.
**임베드만 있고 여는 링크가 없으면 검사가 실패한다.** 학교망에서 막히면 아무것도 안 보이기 때문이다.

**다시 만드는 순서** (하나라도 빠뜨리면 캡처가 옛것으로 남는다)

```
py -3 build/make_webapp.py all      # 앱을 고쳤으면
node build/make_shots.js            # 앱 화면 캡처
node build/make_refshots.js         # 참고자료 누리집 미리보기 (인터넷 필요)
py -3 build/make_ppt.py all         # PPT (캡처를 그림으로 넣는다)
py -3 build/make_lesson_hwpx.py all
py -3 build/make_worksheet_hwpx.py all
py -3 build/make_site.py            # QR·안내·참고자료·슬라이드·인쇄물을 한 번에
py -3 build/verify_all.py
```

**알아 둘 것**

- 구글 드라이브 위에서 돌리면 파일이 동기화 중일 때 `BadZipFile` 이나 `Permission denied` 가 난다.
  결함이 아니다. 잠시 뒤 다시 돌리면 된다. 배포본 복사는 `robocopy /MIR` 가 안전하다.
- `audit.py` 는 파일을 수천 개 읽어 180초를 넘길 때가 있다. 다른 빌드와 동시에 돌리지 않는다.

---

### 10-9. 2026-08-19 낮 : 글자 키우기와 지도안 연결

| 무엇 | 왜 | 어디 |
|---|---|---|
| 활동지 글자를 13.5 → **16.5px** 로 키움 | 초등 5·6학년이 읽고 연필로 쓰는 종이다 | `build/make_view.py` PRINT_CSS |
| 쓰는 줄 간격 22 → **34px** | 손글씨가 들어갈 자리가 없었다 | 같은 파일 |
| 칸마다 무엇을 쓰는지 한 줄과 노란 예시 상자 | 초등은 예시가 있어야 첫 줄을 쓴다 | 같은 파일 |
| 웹앱 본문 19 → **21px**, 버튼 높이 52 → 58px | 태블릿과 교실 뒤에서 읽히게 | `build/webapp_core.py` |
| 지도안 활동마다 **웹앱 화면**을 붙임 | 웹앱 설명이 6절에 따로 있어 전개를 읽다 끊겼다 | `make_view.py` `app_screens()`, `make_lesson_hwpx.py` |
| PPT 에 캡처 8장과 QR, 주소 | pptx 에는 웹앱 흔적이 없었다 | `build/make_ppt.py` |
| 한글 지도안·활동지에 주소 | 한글만 보는 교사가 앱을 못 찾았다 | `make_lesson_hwpx.py`, `make_worksheet_hwpx.py` |

**틀렸다가 고친 것.** 처음에는 캡처 순서(허브·훈련장·분류실)를 활동 순서에 그대로 붙였다.
앱의 여정 순서와 수업 활동 순서는 다르다. "활동 1 = 허브 화면" 같은 틀린 안내가 나왔다.
캡처 이름을 빼고 `lessons.json` 의 `screens` 이름만 쓰도록 고쳤다.

**PPT 장수.** 웹앱 화면을 아홉 장 넣으니 1·2차시가 25장이 되어 규격(10~24장)을 넘었다.
검사 기준을 늘리지 않고, 차시마다 남는 자리만큼만 화면을 넣도록 고쳤다. 1·2차시는 24장이다.

---


### 10-10. 2026-08-20 저녁 : 깃허브 최신화와 라이브 점검


**무엇을 했나.** 저장소 두 곳을 현재 상태로 맞추고, 배포된 사이트를 실제로 눌러 가며 점검했다.

| 저장소 | 무엇이 들어가나 | 이번에 한 일 |
|---|---|---|
| `legoschool/wise-ai` | 배포본 `out/site` 그대로 (GitHub Pages) | 교사용 `README.md` 를 첫 화면에 붙이고, 참고자료 링크 수정본을 올렸다 |
| `legoschool/ai-for-good` | 하네스 전부 (`build` `spec` `data` `out`) | 8월 18일에 멈춰 있던 것을 8월 20일 상태로 맞췄다 (453개 파일) |

`ai-for-good` 의 뿌리 `index.html` 은 `out/site/index.html` 로 넘긴다. 그래서 이 저장소도
https://legoschool.github.io/ai-for-good/ 로 같은 사이트를 연다. 사람에게 알리는 주소는 `wise-ai` 하나로 한다.

**라이브에서 확인한 것 (2026-08-20 22시)**

| 무엇 | 결과 |
|---|---|
| 사이트 내부 링크 257개 (HTML 86쪽 포함) | 전부 200. 끊긴 링크 없음 |
| 웹앱 13개 (12차시 + 공통 설문) | 전부 열리고 `둘러보기` 로 활동 화면까지 들어간다. 콘솔 오류 없음 |
| 교사 방 만들기 → 학생 입장 → 제출 → 교사 화면 | 왕복 성공. 설문 방 `505609` 에 제출 1건이 바로 보였다 |
| 빈 채로 제출 | "아직 답하지 않은 문항이 8개 있어요" 로 막힌다 |
| 관리자 화면 (`admin.html`) | 시트에서 학급 4개를 불러오고 사전·사후 집계와 차시별 참여가 나온다 |
| 슬라이드 (`deck/L06.html`) | 30장, 좌우 키로 넘어간다 |
| 폭 375px (휴대폰) | 홈·웹앱·차시·안내·참고자료·관리자 모두 가로 스크롤 없음 |
| 참고자료 바깥 링크 31개 | 30개 살아 있음. 1개 404 → 아래에서 고쳤다 |
| `verify_all.py` | 83개 중 82개 통과. `audit.py` 만 드라이브 지연으로 180초 초과 (결함 아님) |

**고친 것.** 12차시 참고자료 "초등 학습으로의 평가 이해하기" 주소가 404 였다.
경기도교육청이 게시판 번호를 옮겨 놓았다. `BBSMSTR_000000030222` → `BBSMSTR_000000030136` 으로 고치고
`make_site.py` 로 다시 만들었다. 바뀐 쪽은 `refs.html` `lesson/L12.html` `guide/L12.html` 셋이다.

**정정.** 10-7 절 끝의 "Apps Script 를 새로 붙여 넣고 다시 배포해야 한다"는 이미 끝나 있었다.
지금 배포 주소는 `방목록` 을 되돌려 주고 관리자 화면이 시트를 읽는다. 위 표가 그 증거다.

**남겨 둔 시험 방.** 점검하며 만든 방이다. 시트와 Firebase 에서 지워도 된다.

- `941205` 1차시 데이터 실험실 (제출 없음)
- `505609` 공통 설문 (닉네임 `점검이` 제출 1건, 학생 코드 `0820`)

**알아 둘 것 두 가지**

- 관리자 화면 암호 `wise2026` 은 공개된 `admin.html` 안에 그대로 적혀 있다.
  화면 안내대로 **가벼운 잠금**일 뿐이다. 실제 보호는 방 번호와 Firebase 규칙이 한다.
- 공통 설문 앱에서도 교사 화면의 `낱낱이 보기` 를 켜면 닉네임별 응답이 보인다.
  기본은 꺼져 있고 집계만 나오지만, "설문은 집계만" 규칙을 엄격히 지키려면
  설문 앱에서는 이 단추를 빼는 것을 검토한다.

**`audit.py` 를 따로 돌린 결과 (제한 없이).** 통과했다. 다만 경고가 하나 붙는다.

```
주의  Apps Script 배포 URL 이 아직 자리표시자다 (2개 파일)
```

**오탐이다.** 걸린 두 파일은 `build/webapp_core.py` 와 `build/make_webapp.py`, 곧 웹앱을 찍어내는 틀이다.
틀에는 `__SHEET_ENDPOINT__` 가 있는 것이 맞고 `make_webapp.py` 가 빌드할 때 실제 주소로 바꿔 넣는다.
산출물(`out/`) 안의 자리표시자는 0건이고, 웹앱 13개에 실제 주소가 박혀 있으며, 시트 기록도 실제로 동작한다.
경고를 없애려면 `audit.py` 의 자리표시자 검사에서 `build/` 를 빼면 된다. 검사 범위를 좁히는 일이라 손대지 않았다.

### 10-11. 2026-08-20 밤 : 좁은 화면에서 머리띠가 잘리던 것


**무엇이 보였나.** 참고자료 쪽을 확대해서 보면 위 띠의 `WISE` 와 메뉴 왼쪽이 잘려 나갔다.

**왜 그랬나.** 두 가지가 겹쳤다.

1. **가로로 밀렸다.** `.refgrid` 가 `minmax(300px,1fr)` 이라 화면이 300px 보다 좁으면
   칸이 화면을 넘어간다. 페이지가 옆으로 밀리는데 `.top` 은 붙박이(sticky)라 세로만 따라온다.
   그래서 오른쪽으로 밀린 만큼 머리띠 왼쪽이 화면 밖으로 나갔다.
   같은 까닭으로 안내 쪽의 긴 주소, 인쇄용 쪽의 `210mm` 종이 너비도 화면을 넘겼다.
2. **머리띠가 페이지마다 달랐다.** 참고자료·사용 안내·관리자 쪽은 저마다 머리띠를 따로 적어
   안쪽 폭(`.wrap`)이 없었다. 글자가 화면 맨 왼쪽에 붙어 본문과 줄이 맞지 않았다.

**어떻게 고쳤나**

| 무엇 | 어디 |
|---|---|
| 모든 그리드 최소 너비를 `minmax(min(300px,100%),1fr)` 로 바꿔 화면보다 넓어지지 않게 함 | `site_css.py` 10곳, `make_view.py` 2곳 |
| 긴 주소·긴 낱말이 줄을 바꾸도록 `overflow-wrap` 을 기본으로 깔음 | `site_css.py` 머리 부분 |
| 인쇄용 쪽은 **화면에서만** 종이 너비를 풀도록 함 (`@media screen and (max-width:860px)`) | `make_view.py` PRINT_CSS |
| 머리띠·바닥글을 **한 곳에서만** 만들도록 `site_nav.py` 를 새로 둠 | `site_nav.py` (새 파일) |
| 참고자료·사용 안내 두 쪽이 그 머리띠를 쓰게 함 (메뉴 12칸으로 통일, 본문과 왼쪽 줄 맞음) | `make_refs.py`, `make_guide.py` |
| 휴대폰(≤700px)에서 메뉴를 **한 줄로 두고 옆으로 밀어** 보게 함 | `site_css.py`, `make_admin.py` |

메뉴 목록(`NAV`)은 이제 `site_nav.py` 한 곳에만 있다. **새 페이지를 만들 때는 반드시
`site_nav.top_bar()` `foot_bar()` 를 쓴다.** 머리띠를 페이지에 직접 적지 않는다.

**확인한 것.** 쪽 23종 × 너비 10가지(280~1280px) = 230가지를 재어 **가로 밀림 0건**.
머리띠 높이는 휴대폰 78px, 태블릿 160px, 데스크톱 90px 이다.
`check_site.js` 는 페이지 72개·파일 288개·끊긴 링크 없음으로 통과했다.

### 10-12. 2026-08-21 : 이름 · 번호 · 사용 안내


**1. 연구회 이름을 바꿨다.** `G-DEAL A.N.D` → **`G-DEAL A.N.D`**.
이름은 `data/lessons.json` 의 `team` 과 `copyrightLine` 에서 나온다. 하드코딩되어 있던
`make_view.py` `make_harness_docs.py` `update_state.py` 와 문서(README·CLAUDE·STATE·HANDOVER)도 함께 고쳤다.
웹앱·PPT·한글·사이트가 모두 이 문장을 품고 있어 **전체를 다시 만들었다.**

**2. 아이가 번호를 외우지 않게 했다.** 사전·사후를 잇는 네 자리 번호를 학생이 정하고 열두 주 뒤에
다시 쓰는 구조였다. 초등학생이 기억할 리 없다. 안전망을 세 겹으로 바꿨다.

| 겹 | 무엇 | 어디 |
|---|---|---|
| 1 | **앱이 번호를 만든다.** 입장 화면에 이미 채워져 있고 기기에 남는다 | `webapp_core.py` `makeStudentCode()` |
| 2 | **사후에서 별명으로 찾아 준다.** 같은 방에서 같은 별명으로 낸 사전 응답의 번호를 자동으로 이어 붙인다 | `make_webapp.py` `linkPrior()` |
| 3 | **이어보기 명단.** 설문 교사 화면 아래에 별명·번호 표가 생긴다. 사전 뒤에 인쇄해 두면 끝 | `make_webapp.py` `teacherSummary()` |

번호가 없어도 학급 평균에는 그대로 들어간다. 명단에는 응답 내용이 없다.

**3. 둘러보기는 별명 없이 들어간다.** 혼자 체험하는데 별명을 물을 까닭이 없었다.
별명을 비우면 `둘러보는 중` 으로 들어간다.

**4. 사용 안내를 운영 설명서로 다시 썼다.** 세 줄짜리였다. 이제 여덟 마디다.
한눈에 보는 한 시간 흐름 · 하루 전 준비 · 시작할 때 · 교사 화면 단추표 · 학생 화면 흐름 ·
사전·사후 운영 · 기기가 모자랄 때 · 막히면 · 개인정보 약속.
표 네 개와 차시 카드 열두 개가 함께 있다.

**5. 자기인식 진단 표에서 채점 칸을 뺐다.** 화면에서만 뺐다.
1번 문항 역채점은 웹앱·관리자 화면의 계산에 그대로 살아 있다 (`lessons.json` 의 `scoring` 도 그대로 둔다).

**6. 메뉴 차례를 바꿨다.** 홈 · 둘러보기 · 인간중심 사고 · 자기인식 진단 · 발견 · 판단 · 실천 ·
12차시 웹앱 · 사용 안내 · 참고자료 · 관리자 · 소개. 차례는 `site_nav.py` 의 `NAV` 한 곳에서만 고친다.

**확인한 것.** 배포된 사이트에서 직접 눌러 보았다.
별명 없이 둘러보기 진입, 번호 자동 생성, 사후에서 별명으로 사전 기록 찾기(방 `505609`, 번호 `0820`) 모두 동작했다.
`check_site.js` 는 페이지 72개·파일 288개·끊긴 링크 없음으로 통과했다.

### 10-13. 2026-08-21 : 가상 학급 30명으로 왕복 점검


**무엇을 했나.** 진해신항초 6학년 1반을 가정해 학생1~학생30 을 만들고,
배포된 사이트에서 **실제 앱을 눌러** 12차시 360건과 사전·사후 설문 60건을 넣었다.
방마다 학급 이름표를 달았다. 결과는 `점검기록/` 폴더의 엑셀과 보고서에 있다.

| 무엇 | 결과 |
|---|---|
| 차시 앱 12개 | 30명 전원 기록. 빠진 사람 없음 |
| 사전·사후 잇기 | 30명 전원 번호가 이어짐. 학생은 별명만 그대로 썼다 |
| 점수 변화 | 사전 1.94 → 사후 4.08 (5점, 1번 역채점) |
| 시트 서식 | 방목록·설문·제출_통합·차시별 탭 규칙대로 들어감 |
| 관리자 화면 | 학급 목록·설문 집계·차시별 참여 모두 나옴 |

**이 점검이 잡아낸 결함 네 가지. 전부 고쳤다.**

| 결함 | 증상 | 고친 곳 |
|---|---|---|
| 시트 백업 대기열을 보낸 뒤 **통째로 비웠다** | 그 사이 들어온 제출이 사라져 시트 인원이 최대 12명 모자랐다 | `webapp_core.py` `sheetFlush()` · 보낸 만큼만 덜어 내고 열 때 남은 것을 다시 보낸다 |
| 제출 기록에 **입장 화면 번호**를 담았다 | 사후에 찾아낸 번호가 시트에 반영되지 않아 사전과 이어지지 않았다 | `webapp_core.py` `submit()` · 활동이 가진 번호를 우선한다 |
| `linkTried` 를 한 번만 켰다 | 한 기기로 여러 학생이 이어서 할 때 둘째부터 이어보기를 안 했다 | `make_webapp.py` `activityInit()` 에서 초기화 |
| 관리자 화면이 시트를 **12초** 만에 포기 | 제출이 몰린 직후 "시트를 읽지 못했습니다" | `make_admin.py` · 45초로 늘리고 안내 문구 수정 |

**함께 넣은 것.** 선생님 화면에 **학급 이름표** 칸을 두었다. 방을 만들 때 적으면
시트 `방목록` 의 `학급이름표` 칸과 관리자 화면 목록에 그대로 남는다.

**남겨 둔 것.** 첫 번째 결함이 고쳐지기 전에 넣은 기록이라 **시트 차시별 탭에 행이 빠져 있다**
(차시마다 1~12행). 파이어베이스와 점검 엑셀에는 30명이 다 있다.
시트까지 맞추려면 차시 탭과 `제출_통합` 을 비운 뒤 다시 넣어야 한다. 그냥 넣으면 행이 겹친다.
사후 설문도 학생1~8 이 두 번 들어가 있다. 학생코드 1001~1008 인 나중 행이 맞는 것이다.

**점검 방법 메모.** 앱마다 `window.__wiseTest` 통로가 열려 있어
`activityAutofill()` 로 활동을 채우고 화면의 제출 단추를 눌러 돌렸다.
브라우저 하나로 30명을 이어서 돌리면 **한 기기를 모둠이 돌려 쓰는 상황과 같아진다.**
위 결함 세 개가 전부 그 상황에서만 드러났다. 다음에도 이 방법으로 점검한다.

### 10-14. 2026-08-21 : 뒷정리 (이름·시트·검사기)


**1. 옛 이름을 끝까지 지웠다.** `티처스랩 5기 교사연구회 A.N.D` 가 규격서·프롬프트·체크리스트·
Apps Script 머리말까지 남아 있었다. 하네스 전체에서 **38개 파일**을 `G-DEAL A.N.D` 로 고쳤다.
이제 산출물과 문서 어디에도 옛 이름이 없다.

**2. 시트에 닿지 못한 28건을 채웠다.** 파이어베이스를 기준으로 시트에 없는 것만 골라 다시 보냈다.
**차시 열두 탭이 모두 30명**이 되었다. 시트는 `gviz` 로 탭마다 CSV 를 받아 대조했다.

```
https://docs.google.com/spreadsheets/d/<시트ID>/gviz/tq?tqx=out:csv&sheet=<탭이름>
```

이 주소는 로그인 없이도 읽힌다. 다음에 시트와 파이어베이스를 맞춰 볼 때도 이 방법을 쓴다.

**3. 시트 정리 도구를 만들었다.** 겹친 행을 지우는 일은 웹앱 쪽에서 할 수 없다(추가만 된다).
그래서 `build/apps_script/wise_sheet.gs` 에 함수 셋을 넣었다. 시트 편집기에서 사람이 실행한다.

| 함수 | 하는 일 |
|---|---|
| `점검하기` | 탭마다 행 수·사람 수·겹친 행 수를 보여만 준다 |
| `겹친행지우기` | 시각까지 똑같은 행만 지운다 |
| `최신만남기기` | 방·닉네임(설문은 사전·사후)마다 마지막 제출만 남긴다 |

붙여 넣고 실행하는 법은 `build/apps_script/README.md` 7절에 있다.

**4. 검사기를 고쳤다.**

| 무엇 | 왜 | 결과 |
|---|---|---|
| `audit.py` 가 검색어마다 파일 전체를 **여섯 번** 읽었다 | 드라이브에서 십 분 넘게 걸리고 `verify_all` 이 시간 초과로 NG 를 냈다 | 한 번만 읽고 전부 본다. 여러 갈래로 나눠 읽는다 |
| 자리표시자 검사가 **틀과 점검기 자신**까지 봤다 | 늘 뜨는 오탐 주의가 붙었다 | 산출물(`out/`)만 본다. **주의 0건** 이 되었다 |
| `verify_all` 이 검사 하나에 180초만 줬다 | 드라이브가 느리면 정상인데도 NG | 1500초로 늘렸다 |

전수 대조(`audit.py`)는 드라이브 위에서 **11분쯤** 걸린다. 결함이 아니다.

### 10-15. 2026-08-22 : 시트를 실제로 정리했다


**한 일.** 브라우저로 시트의 Apps Script 편집기를 열어 정리 함수를 붙이고 직접 돌렸다.
먼저 드라이브에 **사본**(`2026. ai적정활용 티처스랩 개발 (정리 전 사본 2026-08-21)`)을 떠 두었다.

| 단계 | 결과 |
|---|---|
| `점검하기` | 겹친 행 : 설문 60 · 제출_통합 666 · 차시 합계 711 |
| `겹친행지우기` | 위 행 전부 지움 |
| `최신만남기기` | 사후 설문 겹침 8행 지움 |
| 빠진 40건 다시 넣기 | 파이어베이스에서 골라 `제출_통합` 을 30건씩 채움 |
| `코드되살리기` | 앞자리 0 이 떨어진 방코드·학생코드 되살림 |
| `빈행지우기` | 값만 지워지고 남은 줄 없앰 |

**지금 시트 상태 (엑셀로 내려받아 대조)**

- 차시 열두 탭 : 각 **30행 · 30명 · 겹침 0**
- 설문 : **사전 30 · 사후 30**, 사후 학생코드가 사전과 전부 일치
- 제출_통합 : 열두 방 모두 **30건**, 설문 방 30건

**중간에 만든 사고와 고침.** `겹친행지우기` 가 값을 다시 쓰면서 텍스트로 저장돼 있던
`057385` 같은 방코드의 앞자리 0 이 떨어졌다. 차시별 집계가 어긋나 바로 드러났다.
그래서 `코드되살리기` 를 만들어 되살리고, 정리 함수를 돌린 뒤에는 **반드시 이어서 돌리도록**
`build/apps_script/README.md` 7절에 순서를 못 박았다.

**하지 않은 것.** `제출통합채우기` 는 Apps Script 가 파이어베이스를 직접 읽어야 해서
`UrlFetchApp` 권한 승인 창이 떴다. 권한 승인은 사람이 할 일이라 그 함수는 빼고,
대신 웹앱 쪽에서 빠진 40건을 다시 보내는 방법으로 채웠다.

**시트를 읽어 대조하는 법.** 로그인 없이 탭을 CSV 로 받을 수 있다. 형식 추론 때문에
텍스트 칸이 빈칸으로 나올 때가 있으니, 정확히 볼 때는 `export?format=xlsx` 로 통째로 받는다.

```
https://docs.google.com/spreadsheets/d/<시트ID>/export?format=xlsx
```
