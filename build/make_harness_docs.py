# -*- coding: utf-8 -*-
"""하네스 문서(체크리스트·규격·프롬프트)를 파일로 쓴다.

사용법 : py -3 build/make_harness_docs.py
내용을 고치려면 이 파일을 고치고 다시 돌린다.
"""
import io
import os
import sys

import tasks as T

T.setup_console()

FOOT = "\n---\n\n2026년 G-DEAL A.N.D · CC BY-NC-SA\n"

DOCS = {}

# ============================== 체크리스트 ==============================

DOCS["loops/checklists/CL1_지도안.md"] = u"""# CL1. 지도안 완료 정의

작업 : `L<NN>/지도안` · 산출 : `out/지도안/WISE_L<NN>_지도안.hwpx`

확인하지 않고 `[x]` 를 채우지 않는다. "아마 맞을 것"은 실패로 본다.

## 파일
- [ ] `assets/base_지도안.hwpx` 를 복사해 제자리 수정했다. 빈 상태에서 새로 조립하지 않았다.
- [ ] `mimetype` 이 zip 첫 항목이고 무압축(STORED)이다.
- [ ] 고친 문단의 `linesegarray` 를 전부 지웠다.
- [ ] `py -3 build/validate_hwpx.py` 가 OK 를 냈다.

## 내용
- [ ] 학습 주제와 학습 문제가 `lessons.json` 과 글자 그대로 같다.
- [ ] 도입 5분 · 전개 30분 · 정리 5분 구조가 살아 있다.
- [ ] 전개 활동이 2개 이상이다.
- [ ] 교사 발문마다 예상 답변이 최소 1개 있다.
- [ ] 중점 휴먼스킬 2개의 지식·이해 / 과정·기능 / 가치·태도가 모두 있다.
- [ ] 준비물 부호 `☆` `★` `△` 가 각각 최소 1번 나온다.
- [ ] 이 차시의 웹앱 이름이 정확하다. 옛 표기(웹앱1·2·3)가 없다.
- [ ] 다른 차시의 학습 문제가 섞여 있지 않다.

## 표기
- [ ] em dash(—)를 쓰지 않았다.
- [ ] 옛 3단계 신호등 표현(허용·조건부·제한)이 없다.
- [ ] 교사용 문장은 평서형, 학생 발문은 청유형이다.
""" + FOOT

DOCS["loops/checklists/CL2_활동지.md"] = u"""# CL2. 활동지 완료 정의

작업 : `L<NN>/활동지` · 산출 : `out/활동지/WISE_L<NN>_활동지.hwpx`

## 파일
- [ ] 원본의 단순 문단만 복제했다. 표를 복제하지 않았다.
- [ ] `mimetype` 이 zip 첫 항목이고 무압축이다.
- [ ] `linesegarray` 를 전부 지웠다.
- [ ] `py -3 build/validate_hwpx.py` 가 OK 를 냈다.

## 내용
- [ ] `lessons.json` 의 `worksheet.sections` 항목이 빠짐없이 있다.
- [ ] 학습 문제가 그대로 있다.
- [ ] 쓸 수 있는 빈 줄이 항목마다 충분하다.
- [ ] 학교·학년·반·모둠·닉네임 칸이 있다. 실명 칸은 없다.
- [ ] 자기점검 문항 4개가 있다.
- [ ] 이 차시의 웹앱 이름과 방 코드 적을 칸이 있다.
- [ ] AI 미사용 대안 활동이 적혀 있다.
- [ ] 개인정보 안내 문구가 있다.

## 표기
- [ ] 한 문장 40자 이내를 기준으로 썼다.
- [ ] 초등 5·6학년이 읽는 낱말만 썼다.
- [ ] em dash(—)를 쓰지 않았다.
""" + FOOT

DOCS["loops/checklists/CL2b_PPT.md"] = u"""# CL2b. PPT 완료 정의

작업 : `L<NN>/PPT` · 산출 : `out/ppt/WISE_L<NN>_수업.pptx`

## 파일
- [ ] 16:9 (13.333in × 7.5in) 이다.
- [ ] 슬라이드가 10~16장이다.
- [ ] 모든 텍스트에 `맑은 고딕` 폰트를 지정했다. 지정하지 않으면 교사 PC에서 깨진다.
- [ ] `py -3 build/validate_pptx.py` 가 OK 를 냈다.

## 내용
- [ ] 표지에 차시 번호·모듈·학습 주제가 있다.
- [ ] 학습 문제 슬라이드가 따로 있다.
- [ ] 전개 활동마다 슬라이드가 하나씩 있다.
- [ ] 웹앱 안내 슬라이드에 앱 이름과 화면 목록이 있다.
- [ ] AI 미사용 대안 활동 슬라이드가 있다.
- [ ] 오늘의 배움 슬라이드에 지식·이해 문장이 있다.
- [ ] 저작권 표기가 있다.

## 발표자 노트
- [ ] **빈 노트가 하나도 없다.**
- [ ] 지도안의 교사 발문이 노트에 전부 들어갔다.
- [ ] 예상 답변도 함께 들어갔다.
- [ ] 준비물과 유의점이 해당 슬라이드 노트에 있다.

## 표기
- [ ] 슬라이드 본문은 요약이다. 지도안 문장을 통째로 붙이지 않았다.
- [ ] em dash(—)를 쓰지 않았다.
""" + FOOT

DOCS["loops/checklists/CL3a_웹앱_구성안.md"] = u"""# CL3a. 웹앱 구성안·프롬프트 완료 정의

작업 : `L<NN>/웹앱 구성안`, `L<NN>/웹앱 프롬프트`
산출 : `out/webapp/L<NN>/SPEC.md`, `out/webapp/L<NN>/PROMPT.md`

**앱보다 프롬프트가 먼저다.** 프롬프트 자체가 산출물이다.
선생님들이 각자 앱을 고쳐 쓸 수 있어야 한다.

## SPEC.md
- [ ] 앱 이름·slug·산출 경로가 `lessons.json` 과 같다.
- [ ] 차시 번호와 학습 문제가 있다.
- [ ] 무엇을 하는 앱인지 한 문단으로 적혀 있다.
- [ ] 화면 목록이 `lessons.json` 의 `webapp.screens` 와 같다.
- [ ] 교사 화면에서 무엇을 보는지 적혀 있다.
- [ ] 저장 형태(payload)가 예시로 있다.
- [ ] 지도 유의점이 빠짐없이 옮겨졌다.
- [ ] 기기가 없을 때 대안이 적혀 있다.

## PROMPT.md
- [ ] 통째로 복사해 붙일 수 있는 코드 블록이 있다.
- [ ] 단일 HTML, 외부 라이브러리·CDN 금지가 명시돼 있다.
- [ ] 방 코드 6자리 + 비밀번호 4자리 + 닉네임 입장이 명시돼 있다.
- [ ] **방 코드 복사 버튼**을 두라는 지시가 있다.
- [ ] **혼자 체험 경로**를 두라는 지시가 있다.
- [ ] **닉네임 중복을 막지 말라**는 지시가 있다.
- [ ] **교사가 나가도 방이 사라지면 안 된다**는 지시가 있다.
- [ ] **정규식에 역슬래시 이스케이프를 쓰지 말라**는 지시가 있다.
- [ ] Firebase 주소·경로와 Sheets 백업(no-cors)이 명시돼 있다.
- [ ] 글자 16px 이상, 터치 44px 이상이 명시돼 있다.
- [ ] 안전 문구 지시가 있다. 10차시는 익명 집계 지시가 추가로 있다.
- [ ] 고쳐 쓰는 법이 적혀 있다.

## 검증
- [ ] `py -3 build/validate_webapp_spec.py "out/webapp/L<NN>"` 가 OK 를 냈다.
""" + FOOT

DOCS["loops/checklists/CL3_웹앱.md"] = u"""# CL3. 웹앱 구현 완료 정의

작업 : `L<NN>/웹앱 구현` · 산출 : `out/webapp/L<NN>/index.html`

`spec/09_웹앱_결함목록.md` 의 실제로 터졌던 10건이 이 목록의 뿌리다.

## 단일 파일
- [ ] HTML 하나로 끝난다. CSS·JS 를 전부 인라인했다.
- [ ] 외부 CDN·폰트·이미지를 참조하지 않는다. 학교망에서 차단된다.
- [ ] Google Sites 소스 코드 삽입에 그대로 붙일 수 있다.

## 실제로 터졌던 것
- [ ] 방 코드 옆에 **복사 버튼**이 있고, `navigator.clipboard` 실패 시 대체 경로가 있다.
- [ ] 교사가 화면을 닫아도 방이 사라지지 않는다. 방 상태는 서버에 있다.
- [ ] **닉네임 중복을 막지 않는다.** 같은 닉네임이면 기존 기록을 이어받는다.
- [ ] 방 코드 없이 들어가는 **혼자 체험** 경로가 있고, 서버에 쓰지 않는다.
- [ ] 정규식에 `\\d` `\\s` `\\w` 를 쓰지 않았다. `[0-9]` 등으로 썼다.
- [ ] 페이지 body 가 가로로 스크롤되지 않는다. 넓은 표는 자체 스크롤 상자에 있다.
- [ ] `window.top` 접근, 새 창 열기, `alert` 남용이 없다.
- [ ] 빈 상태로 제출하면 막고 무엇이 빠졌는지 알려 준다.
- [ ] 저장 실패 시 학생 화면이 멈추지 않고 안내 문구가 뜬다.
- [ ] Sheets 백업 실패가 학생 화면을 막지 않는다.

## 저장
- [ ] Firebase 경로가 `/wise/<slug>/<방코드>/` 아래로 격리돼 있다.
- [ ] 레코드에 `nick` `group` `app` `room` `at` `payload` 6개 키가 있다.
- [ ] Sheets 백업이 `no-cors` + `text/plain` 으로 나간다.
- [ ] `getAnalytics` 를 붙이지 않았다.

## 교사 화면
- [ ] 새 방 만들기, 제출 현황, 집계, CSV 내려받기, 방 잠그기가 있다.
- [ ] 3초 폴링으로 갱신된다. SSE 를 쓰지 않았다.

## 안전·접근성
- [ ] 실명·학번·연락처 입력이 없다. 파일 업로드가 없다.
- [ ] 입장 화면에 개인정보 안내 문구가 있다. (10차시는 전 화면 고정 문구)
- [ ] 기본 글자 16px 이상, 누르는 곳 48px 이상이다.
- [ ] 색만으로 뜻을 구분하지 않는다.
- [ ] `prefers-reduced-motion` 을 존중한다.
- [ ] 저작권 표기가 있다.

## 검증
- [ ] `node build/smoke_webapp.js "out/webapp/L<NN>"` 가 OK 를 냈다.
- [ ] `node build/run_webapp.js "out/webapp/L<NN>"` 가 OK 를 냈다.
""" + FOOT

DOCS["loops/checklists/CL5_사이트.md"] = u"""# CL5. 웹사이트 완료 정의

작업 : `L<NN>/사이트 차시 페이지`, `SITE/웹사이트 구현`
산출 : `out/site/`

## 구조
- [ ] 홈 1, 모듈 3, 차시 12, 웹앱 모음 1, 진단 1, 소개 1 = 19쪽이 있다.
- [ ] 모든 페이지에 같은 머리말 메뉴가 있다.
- [ ] 현재 페이지가 메뉴에서 표시된다.

## 페이지마다
- [ ] `<html lang="ko">` 와 viewport 메타가 있다.
- [ ] `<title>` 이 비어 있지 않고 페이지마다 다르다.
- [ ] 끊긴 링크가 없다.
- [ ] 외부 CDN·폰트를 참조하지 않는다.
- [ ] 저작권 표기가 바닥글에 있다.
- [ ] 가로 스크롤이 생기지 않는다. 표는 자체 스크롤 상자에 있다.

## 차시 페이지
- [ ] 학습 문제가 있다.
- [ ] 중점 휴먼스킬 표(지식·이해 / 과정·기능 / 가치·태도)가 있다.
- [ ] 도입·전개·정리 흐름이 발문과 예상 답변까지 있다.
- [ ] 지도안·활동지·PPT 내려받기 링크가 실제 파일을 가리킨다.
- [ ] 웹앱 열기와 제작 프롬프트 링크가 있다.
- [ ] AI 미사용 대안과 지도 유의점이 있다.
- [ ] 앞 차시·다음 차시 이동이 있다.
- [ ] 다른 차시 내용이 섞여 있지 않다.

## 검증
- [ ] `node build/check_site.js out/site` 가 OK 를 냈다.
- [ ] 산출물을 다시 만든 뒤 `make_site.py` 도 다시 돌렸다.
""" + FOOT

DOCS["loops/checklists/CL6_서류.md"] = u"""# CL6. 진도표·교구·해설서 완료 정의

## 학교자율시간 진도표 (XLSX)
- [ ] 시트 4개 : 진도표, 시수 배분, 평가 계획, 사전사후 설문.
- [ ] 진도표에 12차시가 모두 있고 첫 행이 고정돼 있다.
- [ ] 시수 합계가 13이다. 실과 6 + 도덕 2 + 국어 1 + 자율 4.
- [ ] 학교자율시간·창의적 체험활동이 **4차시(7·8·10·12)** 로 적혀 있다. 3차시가 아니다.
- [ ] 평가 계획이 중점 7역량 전부다.
- [ ] 설문 시트에 변화량 자동 계산 수식과 역채점 안내가 있다.
- [ ] `py -3 build/validate_xlsx.py` 가 OK 를 냈다.

## 카드 교구 3종 (HWPX)
- [ ] 정보 분류 카드 24장(4차시), AI 활용 장면 카드 12장(5차시), 상황 카드 20장(6차시).
- [ ] 자르는 선이 카드마다 있다.
- [ ] 몇 칸으로 나누는지 첫 장에 적혀 있다.
- [ ] 웹앱 없이도 같은 활동을 할 수 있다고 적혀 있다.
- [ ] `py -3 build/validate_hwpx.py` 가 OK 를 냈다.

## 교사용 해설서 (HWPX)
- [ ] 프로그램 개관, 모듈 구성, 4색 신호등, 약속 8조항 뿌리가 있다.
- [ ] 차시별 지도 유의점 12차시가 모두 있다.
- [ ] 부적정 활용 신호와 대응이 있다.
- [ ] 도구 준비 안내가 있다.
- [ ] 평가 기준(상·중·하)이 중점 7역량 전부 있다.
- [ ] 자주 묻는 질문이 있다.
- [ ] `py -3 build/validate_hwpx.py` 가 OK 를 냈다.
""" + FOOT

# ============================== 규격 ==============================

DOCS["spec/04_산출물_규격.md"] = u"""# spec/04. 산출물 규격

## A. 교수·학습 지도안 (HWPX 12편)

경로 : `out/지도안/WISE_L<NN>_지도안.hwpx`

- `assets/base_지도안.hwpx` 를 복사해 **제자리 수정**한다. 새로 조립하지 않는다.
- 표지 문단 10개 + 해당 차시 문단 4개만 남긴다.
- 차시 문단 구성 : 제목 / 모듈·학습문제 / 표 / 빈 문단
- 표 안에 휴먼스킬 표와 도입·전개·정리 표가 들어 있다. 표 구조를 건드리지 않는다.
- 표지는 이 차시 전용으로 바꾼다. 모듈·차시·학습 문제를 넣는다.
- 부호 : `☆` 교사 준비물 · `★` 학생 배부물·웹앱 · `△` 지도 유의점

## B. 학생 활동지 (HWPX 12종)

경로 : `out/활동지/WISE_L<NN>_활동지.hwpx`

- **표를 쓰지 않는다.** 원본의 단순 문단만 복제한다. 표를 복제하면 셀 참조가 어긋난다.
- 구성 : 제목 / 차시·학습문제 / 이름칸 / 활동 항목 / 자기점검 / 웹앱 방코드 / 대안 / 저작권
- 활동 항목은 `lessons.json` 의 `worksheet.sections` 를 그대로 쓴다.
- 항목마다 쓸 수 있는 줄을 4줄 둔다.
- 이름칸은 학교·학년·반·모둠·닉네임이다. **실명 칸을 두지 않는다.**

## C. 수업용 PPT (PPTX 12세트)

경로 : `out/ppt/WISE_L<NN>_수업.pptx`

- 16:9 (13.333in × 7.5in), 10~16장.
- 폰트는 `맑은 고딕` 을 명시적으로 지정한다.
- 슬라이드 순서 : 표지 / 학습문제 / 동기유발 / 전개 활동 n개 / 웹앱 / 대안 / 정리 / 오늘의 배움
- **발표자 노트에 교사 발문과 예상 답변을 그대로 넣는다.** 이것이 지도안과 PPT를 잇는 고리다.
- 슬라이드 본문은 요약이다. 지도안 문장을 통째로 붙이지 않는다.
- 모듈 색 : 모듈1 `#2563eb` · 모듈2 `#d97706` · 모듈3 `#059669`

## D. 학교자율시간 진도표 (XLSX 1종)

경로 : `out/서류/WISE_학교자율시간_진도표.xlsx`

시트 4개다.

| 시트 | 내용 |
|---|---|
| 진도표 | 12차시 × 11열 (차시·모듈·주제·학습문제·성취기준·교과시수·휴먼스킬·산출물·평가·웹앱·운영일) |
| 시수 배분 | 실과 6 + 도덕 2 + 국어 1 + 학교자율시간 4 = 13 |
| 평가 계획 | 중점 7역량 × 상·중·하 |
| 사전사후 설문 | 8문항 + 변화량 자동 계산 수식 |

- 운영 예정일 열은 비워 둔다. 학교마다 다르다.
- 설문 시트의 사전·사후 평균 칸은 노란색으로 표시한다.

## E. 카드 교구 (HWPX 3종)

경로 : `out/교구/`

| 파일 | 장수 | 차시 | 분류 칸 |
|---|---|---|---|
| 정보 분류 카드 | 24 | 4 | 넣어도 돼 / 조건을 지키면 돼 / 넣으면 안 돼 |
| AI 활용 장면 카드 | 12 | 5 | 보조 / 대행 |
| 상황 카드 | 20 | 6 | 초록 / 노랑 / 주황 / 빨강 |

- 카드마다 자르는 선을 둔다.
- 첫 장에 몇 칸으로 나누는지 적는다.
- 두꺼운 종이 인쇄를 전제로 한다.

## F. 교사용 해설서 (HWPX 1종)

경로 : `out/해설서/WISE_교사용_해설서.hwpx`

9개 절이다.

1. 이 프로그램은 무엇인가
2. 모듈 구성
3. AI 신호등 4단계
4. 우리 반 AI 약속 8조항의 뿌리
5. 차시별 지도 유의점 (12차시 전부)
6. 부적정 활용 신호와 대응
7. 도구 준비
8. 평가 (중점 7역량 상·중·하)
9. 자주 묻는 질문
""" + FOOT

DOCS["spec/05_디자인_시스템.md"] = u"""# spec/05. 디자인 시스템

웹앱 12개와 통합 사이트가 같은 얼굴을 하도록 값을 한곳에 모았다.

## 색

| 이름 | 값 | 쓰는 곳 |
|---|---|---|
| 잉크 | `#111827` | 제목 |
| 본문 | `#374151` | 본문 |
| 흐림 | `#6b7280` | 보조 설명 |
| 선 | `#e5e7eb` | 테두리 |
| 종이 | `#ffffff` | 카드 배경 |
| 바탕 | `#f9fafb` | 페이지 배경 |
| 브랜드 | `#1d4ed8` | 링크, 강조 |
| 모듈1 | `#2563eb` | 발견 |
| 모듈2 | `#d97706` | 판단 |
| 모듈3 | `#059669` | 실천 |

신호등 색은 `data/lessons.json` 의 `signals[].color` 를 쓴다.
초록 `#16a34a` · 노랑 `#eab308` · 주황 `#f97316` · 빨강 `#dc2626`

**색만으로 뜻을 구분하지 않는다.** 신호등은 색과 글자를 함께 쓴다.

## 글자

```css
font-family: "Pretendard","Malgun Gothic","맑은 고딕",system-ui,sans-serif;
```

- 웹앱 본문 16px, 학생 입력 18px
- 사이트 본문 17px, 모바일 16px
- 줄 간격 1.7 (웹앱 1.6)
- 제목은 700, 강조는 800

**웹폰트를 내려받지 않는다.** 학교망에서 차단되고 로딩이 느리다.

## 간격과 모양

- 모서리 : 카드 16px, 버튼·입력 10~11px, 알약 999px
- 카드 안 여백 : 사이트 24~26px, 웹앱 18px
- 최대 폭 : 사이트 1080px, 웹앱 1000px

## 누르는 것

- 최소 크기 48px × 48px
- 기본 버튼은 채움, 보조는 테두리(`ghost`), 소극적인 것은 회색(`plain`)
- 비활성은 투명도 0.45

## 화면 크기

```css
@media (max-width:640px) { /* 본문 16px, 여백 축소, 메뉴 줄바꿈 */ }
```

- 교사는 노트북, 학생은 태블릿으로 본다.
- **가로 스크롤이 생기면 안 된다.** 넓은 표는 `overflow-x:auto` 상자 안에서만 스크롤한다.

## 명암

- **밝은 배경 하나로 통일한다.** 다크 모드를 만들지 않는다.
  교실 프로젝터에서 어두운 화면은 보이지 않는다.

## 움직임

```css
@media (prefers-reduced-motion:reduce) { *{transition:none!important;animation:none!important} }
```

## 안내 상자

- 주의 상자 : 배경 `#fff7ed` · 테두리 `#fed7aa` · 글자 `#9a3412`
- 성공은 `#15803d`, 경고는 `#b45309`
""" + FOOT

DOCS["spec/06_안전_개인정보_규칙.md"] = u"""# spec/06. 안전과 개인정보 규칙

**이 문서는 다른 모든 요구사항보다 우선한다.**
충돌하면 이 문서가 이긴다. 초등학생이 쓰고 결과물이 외부에 공개되기 때문이다.

## 1. 수집하지 않는 것

- 실명, 학번, 생년월일, 연락처, 이메일, 주소
- 얼굴 사진, 음성, 영상
- 학생이 올린 파일 (업로드 기능을 만들지 않는다)

## 2. 수집하는 것

- 닉네임 (학생이 정한 별명)
- 모둠 이름
- 활동 응답 (payload)
- 방 코드와 제출 시각

닉네임에 실명을 쓰지 않도록 입력 안내에 적는다.

## 3. 저장과 삭제

- Firebase 실시간 DB `/wise/<slug>/<방코드>/` 아래로만 쓴다.
- 같은 기록을 Google Sheets 에 백업한다.
- **학년도 종료 후 `/wise` 경로를 삭제한다.**
- 백업 시트는 학급 단위 통계만 남기고 개별 행을 지운다.
- 분석은 학급 단위 통계만 쓴다. 개인을 특정하는 분석을 하지 않는다.

## 4. 공개

- 게시판(8차시)은 **교사 승인 후에만** 공개한다.
- 학생 얼굴·이름이 드러나는 자료를 올리지 않는다.
- 공개 배포물에는 재구성한 문장과 출처 표기만 싣는다.
- 공유 금지 표시가 있는 회의자료와 개인 응답이 담긴 델파이 조사지는 내부 참고용으로만 쓴다.

## 5. 심리·정서적 안전 (10차시)

10차시는 사용 습관을 묻는다. 정서적 어려움이 드러날 수 있다.

- **응답을 익명으로 처리한다.**
- **개별 학생을 특정하는 화면을 만들지 않는다.** 학급 익명 집계만 보여 준다.
- 모든 화면 하단에 문구를 고정한다.
  > 힘든 마음은 AI가 아니라 믿을 수 있는 어른에게 먼저 말해요.
- 사용량이 많은 학생을 지적하는 분위기가 되지 않게 한다.
- 도움이 필요해 보이는 학생은 수업 후 개별 면담으로 연결한다.

## 6. 도구 사용

- 생성형 이미지 도구는 **교사 계정으로만** 쓴다.
- 서비스별 사용 가능 연령을 수업 전에 확인한다.
- 학생이 실제 개인정보를 입력하지 않도록 활동 중에 계속 안내한다.
- 사람 얼굴이 담긴 사진을 학습 데이터로 쓰지 않는다.

## 7. 결정된 사항

| 번호 | 쟁점 | 결정 | 날짜 |
|---|---|---|---|
| Q1 | 학생 식별 방식 | 닉네임만 | 2026-08-18 |
| Q2 | 학급 코드 범위 | 학교별 분리 | 2026-08-18 |
| Q3 | 8차시 이미지 업로드 | 기능 없음. 인쇄물·교사 화면 공유로 대체 | 2026-08-18 |
| Q4 | 10차시 위험 신호 표시 | 익명 집계만 | 2026-08-18 |
| Q5 | 웹앱 데이터 저장 | Firebase 실시간 DB + Google Sheets 백업 | 2026-08-18 |

새 쟁점이 생기면 **보수적인 쪽으로 기본 구현**하고 사람에게 묻는다.
사람이 정하면 `py -3 build/update_state.py --decision` 으로 기록한다.
""" + FOOT

DOCS["spec/08_사이트_구조.md"] = u"""# spec/08. 통합 웹사이트 구조

**독립 정적 사이트**로 만든다. 빌드 도구 없이 열리는 순수 HTML/CSS/JS다.

## 참고한 선례

「바이브코딩으로 만나는 사회정서교육」(박창민 외 7명, 광문각) 사이트에서
영역 → 세부 → 차시의 계층 구조와 차시 페이지 구성을 가져왔다.
그 사이트는 32차시 전부에 지도안·활동지·웹앱·PPT를 갖춘 완성 사례다.

우리는 12차시 3모듈이므로 계층을 한 단계 줄였다.

## 페이지 지도

```
out/site/
├─ index.html                 홈
├─ apps.html                  12차시 웹앱 모음
├─ survey.html                AI적정활용 자기인식 진단
├─ about.html                 프로그램 소개
├─ module/
│   ├─ M1.html                모듈1 WISE 발견 (1~4차시)
│   ├─ M2.html                모듈2 WISE 판단 (5~8차시)
│   └─ M3.html                모듈3 WISE 실천 (9~12차시)
├─ lesson/
│   └─ L01.html ~ L12.html    차시 페이지 12쪽
├─ assets/style.css           공통 스타일
├─ files/                     지도안·활동지·PPT 36개
└─ webapp/                    웹앱 12개 + 공통 설문
```

## 머리말 메뉴

홈 · 모듈1 발견 · 모듈2 판단 · 모듈3 실천 · 12차시 웹앱 · 자기인식 진단 · 소개

현재 페이지는 `aria-current="page"` 로 표시한다.

## 홈 구성

1. 표지 : 프로그램 이름, 핵심 질문, 요약 알약 4개
2. 세 개의 모듈 : 모듈 카드 3장
3. 함께 쓰는 자료 : 웹앱 모음 · 진단 · 소개 카드 3장
4. AI 신호등 : 4색 띠
5. 12차시 한눈에 보기 : 차시 목록 12줄

## 차시 페이지 구성

순서를 지킨다.

1. 모듈 띠와 차시 표시
2. 학습 주제와 학습 문제
3. **자료 내려받기** (지도안 · 활동지 · PPT · 웹앱 열기 · 제작 프롬프트)
4. 휴먼스킬 표 (지식·이해 / 과정·기능 / 가치·태도)
5. 수업 흐름 (도입 · 전개 · 정리, 발문과 예상 답변까지)
6. 오늘 쓰는 웹앱
7. 기기가 없어도 함께합니다
8. 지도 유의점 + AI적정활용 연계
9. 교육과정 (성취기준 · 교과시수 · 산출물 · 평가)
10. 앞 차시 / 다음 차시 이동

**자료 내려받기를 위로 올린다.** 선생님이 가장 먼저 찾는 것이다.

## 지켜야 할 것

- 외부 CDN·웹폰트를 참조하지 않는다. 학교망에서 차단된다.
- 모든 페이지 바닥글에 티처스랩 저작권 표기를 유지한다.
- 반응형이어야 한다. 교사는 노트북, 학생은 태블릿으로 본다.
- 가로 스크롤이 생기면 안 된다.
- `data/lessons.json` 에서만 내용을 가져온다. 페이지에 직접 쓰지 않는다.

## 배포

`out/site/` 를 통째로 정적 호스팅에 올린다.
경로가 전부 상대 경로이므로 하위 폴더에 올려도 동작한다.
""" + FOOT

DOCS["spec/09_웹앱_결함목록.md"] = u"""# spec/09. 웹앱 결함 목록

「바이브코딩으로 만나는 사회정서교육」 32차시 웹앱을 실제로 교실에서 돌리며
터졌던 결함이다. **모델이 매번 다시 발견하기 어려우므로 체크리스트에 박아 두었다.**

`build/smoke_webapp.js` 가 이 중 자동으로 잡을 수 있는 것을 검사한다.

| # | 터진 것 | 왜 터졌나 | 막는 법 |
|---|---|---|---|
| 1 | 방 코드를 복사할 수 없다 | 복사 버튼이 없고 학생이 6자리를 눈으로 옮겨 적다 틀린다 | 복사 버튼을 둔다. `navigator.clipboard` 실패 시 `textarea` 대체 경로를 둔다 |
| 2 | 교사가 나가면 방이 사라진다 | 방 상태를 브라우저 메모리에 두었다 | 방 정보를 서버 `meta` 에 쓴다. 교사 화면은 조회만 한다 |
| 3 | 닉네임 중복으로 재입장이 막힌다 | 중복 검사를 넣었다 | 중복을 막지 않는다. 같은 닉네임이면 기존 기록을 이어받는다 |
| 4 | 학교망에서 화면이 비어 있다 | CDN 스크립트가 차단됐다 | 외부 참조를 전부 없앤다. 단일 HTML 로 만든다 |
| 5 | Apps Script 로 옮기니 정규식이 깨진다 | `Index.html` 의 역슬래시가 파괴된다 | `\\d` `\\s` `\\w` 를 쓰지 않는다. `[0-9]` 등 문자 클래스로 쓴다 |
| 6 | 실시간 갱신이 자꾸 끊긴다 | SSE 스트리밍이 학교망 프록시에서 끊긴다 | 3초 폴링으로 바꾼다 |
| 7 | 제출 버튼을 눌러도 아무 일이 없다 | 검증 실패를 조용히 삼켰다 | 무엇이 빠졌는지 화면에 알려 준다 |
| 8 | 인터넷이 끊기면 학생 화면이 멈춘다 | 저장 실패를 처리하지 않았다 | 로컬에 먼저 저장하고 안내 문구를 띄운다. 백업은 대기열에 쌓는다 |
| 9 | 태블릿에서 표가 화면 밖으로 나간다 | 표에 최소 폭을 주고 감싸지 않았다 | `overflow-x:auto` 상자로 감싼다. body 는 가로 스크롤되지 않게 한다 |
| 10 | 프로젝터에서 글씨가 안 보인다 | 다크 모드와 작은 글자를 썼다 | 밝은 배경 하나로 통일한다. 본문 16px 이상, 누르는 곳 48px 이상 |

## 자동으로 잡는 것

`node build/smoke_webapp.js <폴더>` 가 1·3·4·5·9·10 을 잡는다.
`node build/run_webapp.js <폴더>` 가 7 을 잡는다. DOM 을 흉내 내 실제로 실행한다.

2·6·8 은 스크립트로 잡기 어렵다. 체크리스트에서 사람이 확인한다.
""" + FOOT

# ============================== 프롬프트 ==============================


def prompt_doc(title, task, output, spec_refs, script, rules, checklist):
    L = ["# %s" % title, "",
         "작업 : `%s`" % task,
         "산출 : `%s`" % output,
         "규격 : %s" % spec_refs,
         "생성 : `%s`" % script,
         "", "---", "", "## 무엇을 만드는가", ""]
    L += rules
    L += ["", "## 만드는 법", "",
          "```bash",
          "py -3 %s" % script if script.endswith(".py") else script,
          "```", "",
          "이 스크립트가 `data/lessons.json` 하나에서 산출물을 만든다.",
          "**산출물을 직접 고치지 않는다.** 내용을 바꾸려면 `lessons.json` 을 고치고 다시 만든다.",
          "규칙이 바뀌면 이 문서와 스크립트를 함께 고친다.",
          "", "## 끝났는지 어떻게 아는가", "",
          "`%s` 를 한 항목씩 실제로 확인하며 채운다." % checklist,
          "그다음 `next_task.py` 가 알려 준 자동 검증 명령을 실행한다."]
    return "\n".join(L) + "\n" + FOOT


DOCS["prompts/P1_지도안.md"] = prompt_doc(
    "P1. 교수·학습 지도안", "L<NN>/지도안", "out/지도안/WISE_L<NN>_지도안.hwpx",
    "`CLAUDE.md` 3-1절, `spec/04_산출물_규격.md` A절", "build/make_lesson_hwpx.py <차시번호>",
    ["차시 하나의 교수·학습 지도안 HWPX 다.", "",
     "- **원본을 복사해 제자리 수정한다.** 빈 상태에서 새로 조립하면 한글이 보안 경고를 띄운다.",
     "- 표지 문단 10개 + 해당 차시 문단 4개만 남기고 나머지 차시를 지운다.",
     "- 표지를 이 차시 전용으로 바꾼다. 모듈·차시·학습 문제를 넣는다.",
     "- 고친 문단의 `linesegarray` 를 지운다. 안 지우면 글자가 겹쳐 보인다.",
     "- 옛 표기(웹앱1·2·3)를 이 차시의 실제 웹앱 이름으로 바꾼다.",
     "- 도입 5분 · 전개 30분 · 정리 5분 구조와 표를 그대로 둔다.",
     "- 교사 발문마다 예상 답변이 있어야 한다.",
     "- 부호 `☆` 교사 준비물 · `★` 학생 배부물·웹앱 · `△` 지도 유의점 을 유지한다."],
    "loops/checklists/CL1_지도안.md")

DOCS["prompts/P2b_활동지.md"] = prompt_doc(
    "P2b. 학생 활동지", "L<NN>/활동지", "out/활동지/WISE_L<NN>_활동지.hwpx",
    "`CLAUDE.md` 3-1절, `spec/04_산출물_규격.md` B절", "build/make_worksheet_hwpx.py <차시번호>",
    ["차시 하나의 학생 활동지 HWPX 다. 인쇄해서 나눠 준다.", "",
     "- **표를 쓰지 않는다.** 원본의 단순 문단만 복제한다.",
     "  표를 복제하면 셀 참조가 어긋나 한글이 보안 경고를 띄운다.",
     "- 항목은 `lessons.json` 의 `worksheet.sections` 를 그대로 쓴다.",
     "- 항목마다 쓸 수 있는 줄을 4줄 둔다.",
     "- 이름칸은 학교·학년·반·모둠·닉네임이다. **실명 칸을 두지 않는다.**",
     "- 자기점검 문항 4개를 넣는다. 델파이 원칙의 학습자용 확인문항을 학생 언어로 옮긴 것이다.",
     "- 이 차시 웹앱 이름과 방 코드를 적을 칸을 둔다.",
     "- AI 미사용 대안 활동을 적는다.",
     "- 한 문장 40자 이내, 초등 5·6학년이 읽는 낱말만 쓴다."],
    "loops/checklists/CL2_활동지.md")

DOCS["prompts/P2_PPT.md"] = prompt_doc(
    "P2. 수업용 PPT", "L<NN>/PPT", "out/ppt/WISE_L<NN>_수업.pptx",
    "`CLAUDE.md` 3-4절, `spec/04_산출물_규격.md` C절", "build/make_ppt.py <차시번호>",
    ["차시 하나의 수업용 PPTX 다. 교사가 화면에 띄우고 수업한다.", "",
     "- 16:9 (13.333in × 7.5in), 10~16장.",
     "- 폰트를 `맑은 고딕` 으로 **명시적으로 지정한다.** 지정하지 않으면 교사 PC에서 깨진다.",
     "- 순서 : 표지 / 학습문제 / 동기유발 / 전개 활동 n개 / 웹앱 / 대안 / 정리 / 오늘의 배움",
     "- **발표자 노트에 교사 발문과 예상 답변을 그대로 넣는다.**",
     "  이것이 지도안과 PPT를 잇는 고리다. 빈 노트가 하나도 없어야 한다.",
     "- 슬라이드 본문은 요약이다. 지도안 문장을 통째로 붙이지 않는다.",
     "- 모듈 색을 띠와 강조에 쓴다. 모듈1 파랑 · 모듈2 주황 · 모듈3 초록.",
     "- 밝은 배경으로 만든다. 교실 프로젝터에서 어두운 화면은 보이지 않는다."],
    "loops/checklists/CL2b_PPT.md")

DOCS["prompts/P3a_웹앱_구성안과_프롬프트.md"] = prompt_doc(
    "P3a. 웹앱 구성안과 제작 프롬프트",
    "L<NN>/웹앱 구성안, L<NN>/웹앱 프롬프트",
    "out/webapp/L<NN>/SPEC.md, out/webapp/L<NN>/PROMPT.md",
    "`spec/07_웹앱_공통사양.md` 전체", "build/make_webapp.py <차시번호>",
    ["**앱보다 프롬프트가 먼저다.** SPEC.md(구성안) → PROMPT.md(완성 프롬프트) → index.html 순서로 만든다.", "",
     "프롬프트 자체가 산출물이다. 선생님들이 각자 앱을 고쳐 쓸 수 있어야 한다.", "",
     "### SPEC.md 는 무엇을 담는가", "",
     "- 앱 이름·slug·경로, 차시와 학습 문제",
     "- 무엇을 하는 앱인지 한 문단",
     "- 화면 목록 (`lessons.json` 의 `webapp.screens`)",
     "- 교사 화면에서 무엇을 보는지",
     "- 저장 형태(payload) 예시",
     "- 지도 유의점과 기기가 없을 때 대안", "",
     "### PROMPT.md 는 무엇을 담는가", "",
     "통째로 복사해 AI 도구에 붙일 수 있는 코드 블록 하나다. 그 안에",
     "수업 맥락, 화면, 반드시 지킬 것 11가지, 저장 방식, 안전 문구, 말투 규칙이 들어간다.",
     "`spec/09_웹앱_결함목록.md` 의 10건이 '반드시 지킬 것' 의 뿌리다."],
    "loops/checklists/CL3a_웹앱_구성안.md")

DOCS["prompts/P3_웹앱.md"] = prompt_doc(
    "P3. 웹앱 구현", "L<NN>/웹앱 구현", "out/webapp/L<NN>/index.html",
    "`spec/07_웹앱_공통사양.md`, `spec/05_디자인_시스템.md`, `spec/09_웹앱_결함목록.md`",
    "build/make_webapp.py <차시번호>",
    ["Google Sites 소스 코드 삽입용 **단일 HTML 파일**이다. Apps Script 웹앱이 아니다.", "",
     "12개 앱이 공통 골격(`build/webapp_core.py`)을 쓰고 활동 화면만 다르다.",
     "활동 화면은 5종이다 (`build/webapp_activities.py`).", "",
     "| 종류 | 쓰는 차시 |", "|---|---|",
     "| `buckets` 카드 분류 | 4 · 5 · 6 |",
     "| `form` 단계별 기록 | 1 · 2 · 3 · 9 · 11 · 12 |",
     "| `vote` 제안과 투표 | 7 |",
     "| `board` 승인 게시판 | 8 |",
     "| `selfcheck` 익명 자가 점검 | 10 |", "",
     "### 절대 어기지 않는 것", "",
     "- 외부 CDN·웹폰트를 참조하지 않는다. 학교망에서 차단된다.",
     "- 정규식에 `\\d` `\\s` `\\w` 를 쓰지 않는다. Apps Script 가 역슬래시를 파괴한다.",
     "- 방 코드 복사 버튼, 혼자 체험 경로, 교사 대시보드를 반드시 둔다.",
     "- 닉네임 중복을 막지 않는다. 교사가 나가도 방이 사라지지 않는다.",
     "- 실명·학번·연락처를 묻지 않는다. 파일 업로드를 만들지 않는다.",
     "- Firebase 는 REST 로 직접 호출한다. SDK 를 인라인하지 않는다. 3초 폴링을 쓴다.",
     "- Sheets 백업은 `no-cors` + `text/plain` 이고, 실패해도 학생 화면을 막지 않는다.",
     "- 10차시는 익명 집계만 보여 주고 안전 문구를 전 화면에 고정한다."],
    "loops/checklists/CL3_웹앱.md")

DOCS["prompts/P4_웹사이트_디자인.md"] = u"""# P4. 웹사이트 디자인 시안

작업 : `SITE/웹사이트 디자인 시안` · 산출 : `out/site/design/`

이 작업만 **Claude Design** 에 맡긴다. 아래 블록을 붙여 넣는다.

---

```
초등 교사가 쓰는 수업자료 사이트의 디자인 시안을 만들어 줘.

[사이트]
이름 : 인간중심 사고를 기르는 AI적정활용 수업 WISE
대상 : 초등 5·6학년 담임 교사
성격 : 12차시 수업 꾸러미를 내려받고 웹앱을 여는 자료실

[페이지]
홈 / 모듈 3쪽 / 차시 12쪽 / 12차시 웹앱 모음 / 자기인식 진단 / 소개

[차시 페이지에 들어갈 것]
1. 모듈 띠와 차시 표시
2. 학습 주제와 학습 문제
3. 자료 내려받기 (지도안 · 활동지 · PPT · 웹앱 열기 · 제작 프롬프트)
4. 휴먼스킬 표
5. 수업 흐름 (도입 · 전개 · 정리)
6. 오늘 쓰는 웹앱
7. 기기가 없어도 함께합니다
8. 지도 유의점
9. 교육과정 표
10. 앞 차시 / 다음 차시

[색]
모듈1 발견 #2563eb · 모듈2 판단 #d97706 · 모듈3 실천 #059669
브랜드 #1d4ed8 · 바탕 #f9fafb · 종이 #ffffff
신호등 초록 #16a34a 노랑 #eab308 주황 #f97316 빨강 #dc2626

[지켜야 할 것]
- 외부 웹폰트를 쓰지 마라. 학교망에서 차단된다.
- 밝은 배경 하나로 통일해라. 다크 모드를 만들지 마라.
  교실 프로젝터에서 어두운 화면은 보이지 않는다.
- 본문 17px 이상, 누르는 곳 48px 이상.
- 색만으로 뜻을 구분하지 마라.
- 가로 스크롤이 생기면 안 된다.
- 교사가 가장 먼저 찾는 것은 자료 내려받기다. 위쪽에 둬라.
- em dash 를 쓰지 마라.

[내가 원하는 것]
홈과 차시 페이지의 시안, 그리고 색·글자·간격·버튼 값을 정리한 표.
```

---

시안이 나오면 `out/site/design/` 에 저장하고 상태를 완료로 바꾼 뒤,
`spec/05_디자인_시스템.md` 를 갱신하고 `prompts/P5_웹사이트_구현.md` 로 넘긴다.

```bash
py -3 build/update_state.py "SITE/웹사이트 디자인 시안" 완료 "시안 확정"
```
""" + FOOT

DOCS["prompts/P5_웹사이트_구현.md"] = prompt_doc(
    "P5. 웹사이트 구현",
    "L<NN>/사이트 차시 페이지, SITE/웹사이트 구현", "out/site/",
    "`spec/05_디자인_시스템.md`, `spec/08_사이트_구조.md`", "build/make_site.py",
    ["빌드 도구 없이 열리는 **독립 정적 사이트**다.", "",
     "- 19쪽이다. 홈 1 · 모듈 3 · 차시 12 · 웹앱 모음 1 · 진단 1 · 소개 1.",
     "- 내용은 `data/lessons.json` 에서만 가져온다. 페이지에 직접 쓰지 않는다.",
     "- 스타일은 `assets/style.css` 한 곳에 둔다.",
     "- 외부 CDN·웹폰트를 참조하지 않는다.",
     "- 모든 링크가 실제 파일을 가리켜야 한다. 끊긴 링크가 없어야 한다.",
     "- 산출물을 `out/site/files/` 와 `out/site/webapp/` 으로 복사한다.",
     "  **다른 것을 다시 만들었으면 사이트도 다시 만든다.**",
     "- 모든 페이지 바닥글에 티처스랩 저작권 표기를 유지한다.", "",
     "미리보기용 단일 파일이 필요하면 :", "",
     "```bash", "py -3 build/preview_page.py lesson/L06.html out/preview.html", "```"],
    "loops/checklists/CL5_사이트.md")

DOCS["prompts/P7_서류.md"] = prompt_doc(
    "P7. 진도표·카드 교구·교사용 해설서",
    "공통/학교자율시간 진도표, 공통/카드 교구 3종, 공통/교사용 해설서",
    "out/서류/, out/교구/, out/해설서/",
    "`spec/04_산출물_규격.md` D·E·F절", "build/make_docs.py <진도표|교구|해설서|all>",
    ["세 가지를 만든다.", "",
     "**진도표 (XLSX)** : 시트 4개. 진도표 12차시, 시수 배분(합계 13), 평가 계획(중점 7역량),",
     "사전사후 설문(변화량 자동 계산 수식).",
     "학교자율시간·창의적 체험활동은 **4차시(7·8·10·12)** 다. 계획서의 3차시는 오기다.", "",
     "**카드 교구 (HWPX 3종)** : 정보 분류 카드 24장(4차시), AI 활용 장면 카드 12장(5차시),",
     "상황 카드 20장(6차시). 자르는 선을 카드마다 두고, 웹앱 없이도 같은 활동을 할 수 있다고 적는다.", "",
     "**교사용 해설서 (HWPX)** : 9개 절. 개관, 모듈, 4색 신호등, 약속 8조항의 뿌리,",
     "차시별 지도 유의점 12차시, 부적정 활용 신호와 대응, 도구 준비, 평가, 자주 묻는 질문."],
    "loops/checklists/CL6_서류.md")

DOCS["prompts/P6_검수.md"] = u"""# P6. 최종 검수

전체가 끝난 뒤 마지막으로 돌린다.

---

## 1. 자동 검증 전부

```bash
py -3 build/verify_all.py
```

80개 검사가 전부 OK 여야 한다. 하나라도 NG 면 여기서 멈춘다.

## 2. 사람이 확인할 것

자동으로 잡을 수 없는 것이다.

### 한글 파일
- [ ] 지도안 12편을 한글에서 연다. **보안 경고가 뜨지 않는다.**
- [ ] 표 안 글자가 겹쳐 보이지 않는다.
- [ ] 활동지를 인쇄했을 때 쓸 자리가 충분하다.
- [ ] 카드 교구를 인쇄해 자를 수 있다.

### PPT
- [ ] 교사 PC에서 폰트가 깨지지 않는다.
- [ ] 프로젝터에서 뒷자리 학생이 읽을 수 있다.
- [ ] 발표자 노트가 발표자 보기에서 보인다.

### 웹앱
- [ ] 방을 만들고 코드를 복사해 다른 기기로 들어가 본다.
- [ ] **교사 화면을 닫았다가 다시 열어도 방이 살아 있다.**
- [ ] 같은 닉네임으로 다시 들어가면 기록이 이어진다.
- [ ] 인터넷을 끊고 제출해도 학생 화면이 멈추지 않는다.
- [ ] 태블릿에서 가로 스크롤이 생기지 않는다.
- [ ] Google Sites 소스 코드 삽입에 붙여 넣고 동작한다.
- [ ] Firebase 에 기록이 쌓이고 Sheets 에도 같은 행이 들어간다.

### 사이트
- [ ] 홈에서 12차시까지 클릭만으로 갈 수 있다.
- [ ] 자료 내려받기가 실제로 내려받아진다.
- [ ] 휴대폰에서 메뉴가 겹치지 않는다.

## 3. 교육과정 확인

- [ ] 12차시 학습 주제가 계획서와 같다.
- [ ] 신호등이 **4단계**다. 3단계 표현이 남아 있지 않다.
- [ ] 학교자율시간이 **4차시**다.
- [ ] 중점 휴먼스킬 7개가 평가 기준에 연결돼 있다.
- [ ] AI적정활용 8가지 구성요소가 약속 8조항에 대응한다.

## 4. 안전 확인

- [ ] 어떤 화면에도 실명·학번·연락처 입력이 없다.
- [ ] 10차시 앱이 개인 응답을 보여 주지 않는다.
- [ ] 8차시 게시판이 교사 승인 뒤에만 공개된다.
- [ ] 공개 배포물에 델파이 원문이나 공유 금지 자료가 들어가지 않았다.

## 5. 배포 준비

- [ ] Apps Script 를 백업 시트에 붙이고 웹 앱으로 배포했다.
- [ ] 배포 URL 을 `build/make_webapp.py` 의 `SHEET_ENDPOINT` 에 넣고 다시 만들었다.
- [ ] Firebase 보안 규칙을 넣었다.
- [ ] `out/site/` 를 정적 호스팅에 올렸다.

---

## 검수 결과 기록

```bash
py -3 build/update_state.py --decision "최종 검수" "통과" "P6"
```
""" + FOOT


def main():
    written = 0
    for rel, text in sorted(DOCS.items()):
        path = os.path.join(T.ROOT, rel.replace("/", os.sep))
        d = os.path.dirname(path)
        if not os.path.isdir(d):
            os.makedirs(d)
        with io.open(path, "w", encoding="utf-8", newline="\n") as f:
            f.write(text)
        written += 1
        print("  %s" % rel)
    print("")
    print("하네스 문서 %d개를 썼다." % written)
    return 0


if __name__ == "__main__":
    sys.exit(main())
